- 所有智能体与工作流定义均采用 YAML Frontmatter 声明元数据，并使用 XML 标签（如 <process>, <step>）结构化描述执行逻辑。
- 遵循严格的目录读写矩阵（Read/Write Matrix），每个输出目录仅允许单一智能体写入，确保文件所有权清晰。
- 采用 Conventional Commits 规范，提交信息需包含阶段编号（如 01-04）与 QUA 追溯标签，实现原子化版本控制。