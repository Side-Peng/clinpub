- **分层文档体系**：包含架构总览 (`ARCHITECTURE.md`)、快速入门 (`getting-started.md`)、详细配置 (`CONFIGURATION.md`)、开发指南 (`DEVELOPMENT.md`) 和测试规范 (`TESTING.md`)。
- **演进设计记录**：通过 `docs/superpowers/` 目录下的规格书 (`specs/*.md`) 和实施计划 (`plans/*.md`) 管理功能迭代（如 Modify Agent）。
- **示例与验证闭环**：`examples/` 目录提供标准项目配置模板 (`project_config.example.yml`) 和手动集成测试清单 (`04-INTEGRATION-CHECKLIST.md`)，用于验证从数据准备到论文撰写的完整 Phase 0-4 流程。