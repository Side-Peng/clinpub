- 所有统计分析与数据预处理必须以 `cleaned.csv` 为唯一数据源，禁止直接读取原始数据或中间文件。
- 每个分析方法的输出目录必须包含至少一张出版级图片（≥300 DPI）、一张统计表以及一份记录方法细节的 `README.md`。
- 文献检索与引用管理必须确保每条参考文献拥有真实的 DOI，并在 `references.bib` 中采用 Vancouver 格式。
- 智能体在执行关键修改或进入下一阶段前，必须通过检查上游产出的 `MANIFEST.yaml` 来验证输入完整性。