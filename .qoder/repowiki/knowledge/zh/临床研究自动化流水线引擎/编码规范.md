- 所有统计分析必须报告效应量、95%置信区间及精确P值，禁止仅使用'P < 0.05'的模糊表述。
- 可视化图表统一应用`theme_pub()`出版级主题，分辨率不低于300 DPI，且坐标轴标签必须使用英文。
- 跨阶段交付物必须生成`MANIFEST.yaml`，下游智能体在消费前需校验文件存在性及质量约束。
- 手稿撰写采用分段独立生成策略，通过`{{Table:N}}`等占位符进行交叉引用，终稿时执行统一重编号与拼接。
- 数据分析执行依赖感知的波次（Wave）顺序，前一波次用户确认后方可进入下一波次，确保结果的可解释性。