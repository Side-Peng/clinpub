- 所有指令文件均遵循统一的 Frontmatter 结构，包含 name, description, argument-hint 及 allowed-tools 字段。
- 指令执行逻辑通过 `<execution_context>` 引用外部工作流文件，实现指令定义与业务逻辑的解耦。
- 每个指令均定义明确的 `<success_criteria>` 块，用于自动化验证阶段交付物的完整性与合规性。