- 采用 Claude Code `PreToolUse` 钩子架构，通过标准输入接收工具调用上下文并输出 JSON 决策协议（allow/block）。
- 内部划分为三个独立防护维度：Bash 脚本 (`clinpub-phase-boundary.sh`) 负责基于 `STATE.md` 和目录结构的阶段门禁检查；Node.js 脚本 (`clinpub-prompt-guard.js`) 负责扫描数据文件中的提示注入模式；Node.js 脚本 (`clinpub-workflow-guard.js`) 负责根据当前阶段限制文件写入路径。
- 依赖 `.clinpub/STATE.md` 作为权威状态源，并通过 `PROJECT_DIR` 环境变量定位项目根目录及规范化的阶段目录（如 `01_RawData`, `05_Manuscript`）。