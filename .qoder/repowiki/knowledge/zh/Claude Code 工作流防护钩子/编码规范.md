- 所有钩子均遵循统一的 JSON 输出协议，成功时向 stdout 输出 `{"hookSpecificOutput":{"decision":"allow"}}`，失败时向 stderr 输出包含 reason 的 block 决策并以 exit code 2 终止。
- 在解析错误或文件缺失等异常情况下采取“故障开放”（fail-open）策略，默认返回 allow 决策以确保工作流不被意外中断。
- 通过正则表达式或关键词匹配从输入命令或文件路径中提取阶段意图，并映射到规范的流水线阶段编号（Phase 0-4）。