---
phase: 03-手稿拼接
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/references/reference-library.md
autonomous: true
requirements: [WRITE-01, WRITE-02]

must_haves:
  truths:
    - "各段使用全局统一的引用库 JSON 管理引用，不重复引用"
    - "引用采用 Vancouver 编号制 [1][2]，末尾统一 References 区"
    - "写任一段时都查询已有引用，已存在则复用编号（D-09）"
    - "各段使用结构化占位符（{{Table:N}} {{Figure:N}} {{Method:name}}）进行交叉引用"
    - "Table/Figure 全局统一编号，按 IMRAD 顺序递增"
    - "Reference 区在终稿拼接时由库自动生成"
    - "Intro 引用 10-15 篇，Methods 3-5 篇，Results 0-3 篇，Discussion 15-25 篇"
  artifacts:
    - path: "pipeline/references/reference-library.md"
      provides: "Shared reference library JSON schema + placeholder conventions + deduplication rules + cross-reference numbering spec"
      min_lines: 80
    - path: "Reference/reference_library.json"
      provides: "Template for the shared reference library JSON file"
      contains: "reference_library"
  key_links:
    - from: "pipeline/references/reference-library.md"
      to: "Reference/reference_library.json"
      via: "工作流中引用库结构由该文档定义"
      pattern: "reference_library.json"
    - from: "pipeline/references/reference-library.md"
      to: "pipeline/workflows/writing.md"
      via: "各段撰写流程引用该规范管理引用"
      pattern: "reference-library.md"
---

<objective>
创建全局引用管理与交叉引用规范。定义 shared reference library 的 JSON schema、placeholder 命名约定、去重规则、图表全局编号方案。

Purpose: 各段撰写时引用必须统一管理。writer-agent 写任一段时都查询已有引用库（D-09），已存在则不重复添加。交叉引用使用占位符，终稿拼接时统一替换（D-11）。Table/Figure 全局编号（D-12）。

Output:
- `pipeline/references/reference-library.md` — 完整的引用管理规范文档，包含 JSON schema、placeholder 约定、编号策略、去重规则
</objective>

<execution_context>
@./pipeline/workflows/writing.md
@./agents/writer-agent.md
@./agents/reference-agent.md
@./pipeline/references/manifest-format.md
@./.clinpub/phases/03-手稿拼接/03-CONTEXT.md
</execution_context>

<context>
@./pipeline/workflows/writing.md
@./agents/reference-agent.md

<!-- 关键决策 (from 03-CONTEXT.md) -->
<!-- D-07: 末尾统一 References 区，正文标编号，自然去重 -->
<!-- D-08: Vancouver 编号制，正文 [1][2] -->
<!-- D-09: 全局统一编号，共享引用库。写任何段时查询已有引用 -->
<!-- D-10: Intro 10-15, Methods 3-5, Results 0-3, Discussion 15-25 -->
<!-- D-11: 使用占位符 {{Table1}} {{MethodX}}，拼接时替换 -->
<!-- D-12: Table/Figure 全局统一编号，IMRAD 顺序 -->
</context>

<tasks>

<task type="auto">
<name>Task 1: Shared Reference Library JSON Schema 定义</name>
<files>pipeline/references/reference-library.md</files>
<read_first>
- `pipeline/references/manifest-format.md` — 项目已有 YAML schema 风格（第 11-45 行），本文件采用类似的结构化 schema 风格
- `agents/writer-agent.md` — 第 50 行对 Reference/citation_map.md 和 references.bib 的引用（writing agent 当前使用方式）
- `agents/reference-agent.md` — 第 85-89 行 references.bib 输出格式，第 70-74 行 citation_map.md 结构
- `pipeline/workflows/writing.md` — 第 33-44 行参考搜索流程
- `.clinpub/phases/03-手稿拼接/03-CONTEXT.md` — D-07~D-10 的全部细节
</read_first>
<action>
创建 `pipeline/references/reference-library.md`，写入以下内容：

1. **文档 header + purpose 段落**：

```markdown
# Reference Library 规范和交叉引用约定

> Phase 3 spec: 分段撰写时管理文献引用和段间交叉引用的统一规范。
> Writer Agent 和 Reference Agent 共同遵守。

## 文件位置

- **引用库文件**: `Reference/reference_library.json`（工作流运行时生成/更新）
- **本规范文档**: `pipeline/references/reference-library.md`
```

2. **Shared Reference Library JSON Schema 定义**：

```markdown
## 1. Shared Reference Library

### 1.1 JSON 整体结构

```json
{
  "version": "1.0",
  "last_updated": "2026-05-07",
  "references": [
    {
      "id": 1,
      "citation_key": "Author2024",
      "title": "Article Title",
      "authors": ["Author A", "Author B", "Author C"],
      "journal": "Journal Name",
      "year": 2024,
      "volume": "10",
      "issue": "3",
      "pages": "100-110",
      "doi": "10.xxx/xxxxx",
      "pmid": "12345678",
      "sections_used": ["introduction", "discussion"],
      "added_by_section": "introduction",
      "citation_reason": "Supports the association between X and Y"
    }
  ]
}
```

### 1.2 字段说明

| 字段 | 必填 | 说明 |
|------|------|------|
| `id` | 是 | 全局唯一编号，Vancouver 正文编号 [id]。只增不减，删除保留 ID 但标记删除 |
| `citation_key` | 是 | BibTeX 风格 key（AuthorYear），用于去重判断 |
| `title` | 是 | 论文标题 |
| `authors` | 是 | 作者列表，首人姓在前名在后，后续人标准格式 |
| `journal` | 是 | 期刊名缩写或全称 |
| `year` | 是 | 发表年份 |
| `volume` | 否 | 卷号（无则留空） |
| `issue` | 否 | 期号（无则留空） |
| `pages` | 否 | 页码（无则留空） |
| `doi` | 是 | DOI。无 DOI 的引用标记为 `pending_doi`，由用户提供 |
| `pmid` | 否 | PubMed ID |
| `sections_used` | 是 | 该引用被哪些段引用的列表（["introduction", "discussion"]）。去重时检查此字段 |
| `added_by_section` | 是 | 首次添加该引用的段落名称 |
| `citation_reason` | 是 | 引用此文献的原因描述 |

### 1.3 每次写入引用库的规则（D-09）

1. **查询在先**: 添加新引用前，先搜索 `reference_library.json` 的 `citation_key`（AuthorYear）字段
2. **复用编号**: 如果 `citation_key` 已存在 → 复用其 `id`，在 `sections_used` 追加当前段落名。不做重复条目
3. **追加新条目**: 如果 `citation_key` 不存在 → 分配下一个最大 `id+1`，追加到库中
4. **去重键策略**: 以 `citation_key`（AuthorYear）为主键。如果不同论文同 AuthorYear（极少数重复引用场景），加后缀区分：`Author2024a`, `Author2024b`
5. **DOI 必填**: 任何无 DOI 的引用标记为 `pending_doi` 并在段落末尾标注 ⚠️。不可跳过
```

3. **Vancouver 编号和引用格式 (D-07, D-08)**：

```markdown
## 2. Vancouver 引用格式

### 2.1 正文内引用

使用上标或方括号编号：

- 单篇：`[1]`, `[2]`
- 多篇连续：`[1-3]`
- 多篇不连续：`[1,4,7]`
- 作者融入正文：`Smith et al. [1] reported that...`

### 2.2 末尾 References 区格式

末尾统一 References 区，格式如下（Vancouver 风格）：

```
References

[1] Author A, Author B, Author C. Title of the article. Journal Abbreviation. 2024;10(3):100-110. doi:10.xxx/xxxxx
[2] Author D, Author E. Another title. Journal Name. 2023;15(2):50-60. doi:10.yyy/yyyyy
```

### 2.3 段落引用量指南（D-10）

| 段落 | 建议引用数 | 说明 |
|------|-----------|------|
| Introduction | 10-15 篇 | 背景(3-5) + 已知证据(3-5) + gap(2-3) + 目的(1) |
| Methods | 3-5 篇 | 方法学来源、已发表协议、诊断标准引用 |
| Results | 0-3 篇 | 仅当结果需要与已有文献对比时引用 |
| Discussion | 15-25 篇 | 对比(8-12) + 机制(3-5) + 临床意义(2-3) + 局限(1-2) |
```

4. **Cross-reference 占位符规范 (D-11, D-12)**：

```markdown
## 3. 交叉引用占位符

### 3.1 占位符命名规则

各段撰写时使用以下占位符格式，终稿拼接时统一替换为真实编号：

| 占位符 | 含义 | 示例 |
|--------|------|------|
| `{{Table:N}}` | 表格 N (N 为顺序号) | `{{Table:1}}`, `{{Table:2}}` |
| `{{Figure:N}}` | 图 N (N 为顺序号) | `{{Figure:1}}`, `{{Figure:2}}` |
| `{{SupplementaryTable:N}}` | 补充表 N | `{{SupplementaryTable:1}}` |
| `{{SupplementaryFigure:N}}` | 补充图 N | `{{SupplementaryFigure:1}}` |
| `{{Method:name}}` | 方法引用（name 分析方法名） | `{{Method:BaselineTable}}`, `{{Method:SurvivalAnalysis}}` |
| `{{Section:name}}` | 段间引用（跨段交叉引用） | `{{Section:methods}}`, `{{Section:results}}` |

### 3.2 占位符使用规则

1. **主观顺序编号**: 写段时不关心全局编号。在 Methods 的 Table 填 `{{Table:1}}`，在 Results 的 Table 填 `{{Table:2}}` 等。主观编号以段内出现顺序为准
2. **全局重编号**: 拼接时扫描全文所有 `{{Table:N}}`，按 IMRAD 段出现顺序（Methods → Results → Discussion → Intro）重新分配全局编号
3. **Methods 占位符**: Methods 段涉及的分析方法引用用 `{{Method:MethodName}}`，拼接时替换为方法名的真实表述（如 "the baseline characteristics analysis"）
4. **Section 占位符**: 段间交叉引用（如 Discussion 中"详见 Method section"）用 `{{Section:methods}}`，拼接时保留为纯文本
5. **占位符不嵌套**: 不在一个占位符内包含另一个占位符

### 3.3 Table/Figure 全局编号策略（D-12）

Table 和 Figure 各自独立编号，按 IMRAD 顺序：

| 段落顺序 | Table 编号示例 | Figure 编号示例 |
|----------|---------------|-----------------|
| Methods 段第 1 个表 | Table 1 | — |
| Methods 段第 2 个表 | Table 2 | — |
| Results 段第 1 个表 | Table 3 | Figure 1 |
| Results 段第 1 个图 | — | Figure 2 |
| Results 段第 2 个表 | Table 4 | — |

Tables 和 Figures 各自独立递增，不交叉编号。

### 3.4 占位符正则模式（供自动化替换使用）

```
Table:       /{{Table:(\d+)}}/
Figure:      /{{Figure:(\d+)}}/
Method:      /{{Method:(\w+)}}/
Section:     /{{Section:(\w+)}}/
Supplementary Table: /{{SupplementaryTable:(\d+)}}/
Supplementary Figure: /{{SupplementaryFigure:(\d+)}}/
```
```

5. **Reference Library 的写入和读取流程**：

```markdown
## 4. Reference Library 读写流程

### 4.1 每段前的操作流程（由 workflow 编排）

每段撰写开始前（D-06 reference-agent pre-search）：

1. Reference Agent 执行文献搜索（参见 `agents/reference-agent.md`）
2. 如果 `Reference/reference_library.json` 不存在 → 创建初始空库 `{"version":"1.0","last_updated":"...","references":[]}`
3. 读取已有库内容
4. 对新找到的引用逐条检查 `citation_key` 去重（规则见 1.3）
5. 新引用分配 `id`（max_id + 1），写入库
6. 更新 `last_updated` 字段
7. 保存 `Reference/reference_library.json`

### 4.2 各段撰写时的引用使用规则（writer-agent）

Writer Agent 写入某段正文时：

1. 阅读 `Reference/reference_library.json` 获取所有引用库条目
2. 对每个要引用的文献，使用 `{{ref:citation_key}}` 标记（如 `{{ref:Smith2024}}`）
3. 段内引用在正文中以 `[id]` 格式使用（如 Previous studies show that... [1,3-5]）
4. 不用关心 `citation_key` 对应的 id 号是否发生变化，拼接时会统一重编号

### 4.3 终稿拼接时的引用处理（由拼接协议执行）

拼接时：

1. 扫描全文所有 `[id]` 引用标记
2. 解析每个编号对应的引用条目（从 `reference_library.json`）
3. 按正文出现顺序重新分配连续编号（从 [1] 开始）
4. 对使用同一引用的不同段落，复用同一编号（自然去重，D-07）
5. 在全文末尾生成 References 区
6. 更新 `reference_library.json` 的 `sections_used`

### 4.4 MANIFEST.yaml 更新

Reference Agent 完成搜索后，更新 `Reference/MANIFEST.yaml`（参见 `pipeline/references/manifest-format.md`）：
- 在 outputs 中加入 `Reference/reference_library.json`
- 在 handoffs 中声明 writer-agent 为消费者
```

6. **D-10 引用量提示**：

```markdown
## 5. 引用量指南

（D-10 规定：常规学术量）

| 段落 | 数量 | 功能分布 |
|------|------|---------|
| Introduction | 10-15 | 疾病负担 3-5, 现有证据 3-5, gap 2-3, 目的 1 |
| Methods | 3-5 | 方法来源 1-2, 诊断标准 1-2, 统计方法 1 |
| Results | 0-3 | 仅需与已有结果对比时引用 |
| Discussion | 15-25 | 对比讨论 8-12, 机制解释 3-5, 临床意义 2-3, 局限 1-2 |

Reference Agent 搜索时遵循此量级控制，不超标。
writer-agent 写引用时注意段落量级约束。
```
</action>
<acceptance_criteria>
- grep -c "reference_library.json" pipeline/references/reference-library.md 返回 >= 3（JSON schema + 读写流程）
- grep -c "citation_key" pipeline/references/reference-library.md 返回 >= 2（去重主键定义）
- grep -c "{{Table:" pipeline/references/reference-library.md 返回 >= 2（Table 占位符）
- grep -c "{{Figure:" pipeline/references/reference-library.md 返回 >= 2（Figure 占位符）
- grep -c "{{Method:" pipeline/references/reference-library.md 返回 >= 2（方法占位符）
- grep -c "{{Section:" pipeline/references/reference-library.md 返回 >= 2（段间引用占位符）
- grep -c "D-07\|D-08\|D-09\|D-10\|D-11\|D-12" pipeline/references/reference-library.md 返回 >= 6（所有决策引用）
- grep -c "Vancouver" pipeline/references/reference-library.md 返回 >= 2（引用格式）
- grep -c "sections_used" pipeline/references/reference-library.md 返回 >= 2（去重策略）
- grep -c "MANIFEST.yaml" pipeline/references/reference-library.md 返回 >= 1（manifest 更新提及）
</acceptance_criteria>
<verify>
  <automated>grep -c "reference_library.json" pipeline/references/reference-library.md</automated>
  <automated>grep -c "D-07\|D-08\|D-09\|D-10\|D-11\|D-12" pipeline/references/reference-library.md</automated>
  <automated>grep -c "{{Table:" pipeline/references/reference-library.md</automated>
  <automated>grep -c "{{Figure:" pipeline/references/reference-library.md</automated>
  <automated>grep -c "Vancouver" pipeline/references/reference-library.md</automated>
</verify>
<done>reference-library.md 完整创建：JSON schema、去重规则、Vancouver 格式规范、占位符命名约定、Table/Figure 全局编号策略、读写流程</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference_library.json ← writer-agent | writer-agent 读取引用库内容用于引用 |
| reference_library.json ← reference-agent | reference-agent 写入新引用到库 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | reference_library.json 被两个 agent 交叉写入 | mitigate | 编号分配策略明确规定：每个引用分配唯一递增 ID，不重写已有条目。sections_used 字段仅追加不覆盖 |
| T-03-02 | Spoofing | citation_key 冲突导致引用重复 | mitigate | 加后缀策略 `Author2024a`, `Author2024b` 处理极少数同 AuthorYear 场景。去重主键为 `citation_key` + 可选的 `doi` 二次确认 |
| T-03-03 | DoS | 引用库 JSON 损坏 | accept | 段落撰写前读取时的 JSON 解析失败应报告给用户并停止，不自动重建 |
</threat_model>

<verification>
1. `pipeline/references/reference-library.md` 存在且包含 5 个主要章节
2. JSON schema 定义完整（version, last_updated, references[] 结构）
3. 去重规则明确（citation_key 主键，sections_used 记录复用）
4. 占位符类型覆盖 Table/Figure/Method/Section/SupplementaryTable/SupplementaryFigure
5. Table/Figure 全局编号策略明确
6. D-07~D-12 全部引用并实现
7. MANIFEST.yaml 更新方式提及
</verification>

<success_criteria>
- [ ] `grep -c "reference_library.json" pipeline/references/reference-library.md` >= 3
- [ ] `grep -c "D-07\|D-08\|D-09\|D-10\|D-11\|D-12" pipeline/references/reference-library.md` >= 6
- [ ] `grep -c "{{Table:" pipeline/references/reference-library.md` >= 2
- [ ] `grep -c "{{Figure:" pipeline/references/reference-library.md` >= 2
- [ ] `grep -c "{{Method:" pipeline/references/reference-library.md` >= 2
- [ ] 占位符类型覆盖 6 种（Table, Figure, Method, Section, SupplementaryTable, SupplementaryFigure）
- [ ] 去重规则明确（citation_key 主键）
- [ ] Vancouver 格式定义清晰
- [ ] 引用量指南覆盖所有 4 段
</success_criteria>

<output>
After completion, create `.clinpub/phases/03-手稿拼接/03-02-SUMMARY.md`
</output>
