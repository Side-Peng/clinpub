---
phase: 03-手稿拼接
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - pipeline/references/concatenation-protocol.md
  - pipeline/workflows/writing.md
autonomous: true
requirements: [WRITE-02]

must_haves:
  truths:
    - "终稿由各段拼接而成，非重写（WRITE-02）"
    - "各段独立文件保留在 05_Manuscript/sections/ 下"
    - "占位符（{{Table:N}} {{Figure:N}} {{Method:name}} {{Section:name}}）被替换为真实编号"
    - "Table/Figure 按 IMRAD 顺序全局统一编号（D-12）"
    - "引用在合并时统一重编号，使用连续 Vancouver 编号（D-07, D-08）"
    - "重复引用自动去重，同一篇引用在多个段出现时复用同一编号（D-09）"
    - "manuscript.md 包含 YAML frontmatter（title, target_journal, word_count, reference_count）"
    - "MANIFEST.yaml 写入 05_Manuscript/ 声明所有产出"
  artifacts:
    - path: "pipeline/references/concatenation-protocol.md"
      provides: "Concatenation logic specification: section merge, placeholder replacement, citation unification, YAML frontmatter generation, MANIFEST.yaml update"
      contains: "placeholder.*replace|replace.*placeholder"
    - path: "pipeline/workflows/writing.md"
      provides: "Updated writing workflow with concatenation step replacing placeholder step"
      contains: "concatenate_manuscript"
  key_links:
    - from: "pipeline/references/concatenation-protocol.md"
      to: "pipeline/references/reference-library.md"
      via: "引用库读取 + 全局编号重排"
      pattern: "reference-library.md"
    - from: "pipeline/references/concatenation-protocol.md"
      to: "05_Manuscript/sections/"
      via: "读取各段文件进行拼接"
      pattern: "05_Manuscript/sections"
    - from: "pipeline/references/concatenation-protocol.md"
      to: "05_Manuscript/manuscript.md"
      via: "生成完整终稿"
      pattern: "manuscript.md"
    - from: "pipeline/workflows/writing.md"
      to: "pipeline/references/concatenation-protocol.md"
      via: "workflow 调用拼接协议"
      pattern: "concatenation-protocol.md"
---

<objective>
创建终稿拼接逻辑：合并各段独立文件 → 替换占位符 → 统一引用编号 → 生成 manuscript.md（YAML frontmatter）+ sections/ 输出 → 更新 MANIFEST.yaml。

Purpose: 实现 WRITE-02（终稿由各段拼接而成，非重写，引用在合并时统一整理）。各段独立撰写完成后通过拼接协议自动合并为终稿，保持原文不重写，仅做引用统一和交叉引用替换。

Output:
- `pipeline/references/concatenation-protocol.md` — 拼接协议规范文档
- `pipeline/workflows/writing.md` — 追加拼接步骤，替换 Plan 03-01 中的占位符步骤
</objective>

<execution_context>
@./pipeline/workflows/writing.md
@./pipeline/references/reference-library.md
@./pipeline/references/manifest-format.md
@./.clinpub/phases/03-手稿拼接/03-CONTEXT.md
</execution_context>

<context>
@./pipeline/references/reference-library.md
@./pipeline/references/manifest-format.md

<!-- 关键决策 (from 03-CONTEXT.md) -->
<!-- D-07: 末尾统一 References 区，正文标编号，自然去重 -->
<!-- D-08: Vancouver 编号制，正文 [1][2] -->
<!-- D-09: 全局统一编号，共享引用库 -->
<!-- D-11: 使用占位符（{{Table1}} {{MethodX}}），拼接时替换 -->
<!-- D-12: Table/Figure 全局统一编号，IMRAD 顺序 -->
<!-- D-13: Results 风格：描述关键发现 + 指向图表 -->
<!-- D-14: Markdown 格式 -->
<!-- D-15: manuscript.md 终稿 + sections/ 各段独立文件 -->
<!-- D-16: YAML frontmatter: title, target_journal, word_count, reference_count -->

<interfaces>
<!-- 输入文件 -->
各段文件:
  05_Manuscript/sections/01-introduction.md
  05_Manuscript/sections/02-methods.md
  05_Manuscript/sections/03-results.md
  05_Manuscript/sections/04-discussion.md

引用库:
  Reference/reference_library.json

<!-- 输出文件 -->
终稿:
  05_Manuscript/manuscript.md

各段备份:
  05_Manuscript/sections/ (直接引用现有文件，不重新生成)

<!-- 占位符 Regex 模式（来自 reference-library.md 3.4 节） -->
Table:             /{{Table:(\d+)}}/
Figure:            /{{Figure:(\d+)}}/
Method:            /{{Method:(\w+)}}/
Section:           /{{Section:(\w+)}}/
SupplementaryTable: /{{SupplementaryTable:(\d+)}}/
SupplementaryFigure: /{{SupplementaryFigure:(\d+)}}/
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: 创建 concatenation-protocol.md — 完整拼接协议规范</name>
<files>pipeline/references/concatenation-protocol.md</files>
<read_first>
- `pipeline/references/reference-library.md` — Plan 03-02 创建的引用库规范（占位符模式、JSON schema、Vancouver 格式）
- `pipeline/references/manifest-format.md` — MANIFEST.yaml 格式规范，第 126-147 行 Writer Agent manifest 模板
- `pipeline/contexts/writing.md` — 撰写上下文（语言策略、章节顺序、研究类型模板）
- `.clinpub/phases/03-手稿拼接/03-CONTEXT.md` — D-07~D-16 全部决策
</read_first>
<action>
创建 `pipeline/references/concatenation-protocol.md`，写入以下内容：

```markdown
# 终稿拼接协议 (Concatenation Protocol)

> Phase 3: 各段独立撰写完成后，执行本协议合并为终稿。
> 原则：拼接而非重写（WRITE-02），引用统一整理，占位符替换。

## 输入输出

### 输入（由分段撰写流程 `sequential_section_writing` 产生）

| 文件 | 来源 |
|------|------|
| `05_Manuscript/sections/01-introduction.md` | writer-agent 撰写 |
| `05_Manuscript/sections/02-methods.md` | writer-agent 撰写 |
| `05_Manuscript/sections/03-results.md` | writer-agent 撰写 |
| `05_Manuscript/sections/04-discussion.md` | writer-agent 撰写 |
| `Reference/reference_library.json` | reference-agent 逐段更新 |

### 输出

| 文件 | 说明 |
|------|------|
| `05_Manuscript/manuscript.md` | 完整终稿（YAML frontmatter + 合并且重编号） |
| `05_Manuscript/sections/*.md` | 各段独立文件（保留原始文件，不做修改，与 manuscript.md 内的段内容一致） |

---

## 拼接步骤

### Step 1: 段落合并

按 IMRAD 顺序读取各段文件并拼接：

```
merged_content = ""
for section in [introduction, methods, results, discussion]:
    filepath = "05_Manuscript/sections/{序号}-{段名}.md"
    content = read(filepath)
    merged_content += content + "\n\n"
```

段间用一个空行分隔。不修改原文内容——拼接而非重写（WRITE-02）。

各段 IMRAD 顺序（引入到 discussion，与撰写顺序 D-01 一致）：
1. Introduction（01-introduction.md）
2. Methods（02-methods.md）
3. Results（03-results.md）
4. Discussion（04-discussion.md）

注意：不包含 Abstract——Abstract 在 manuscript.md 完成后由 writer-agent 最后撰写（遵循 writer-agent.md 的 `draft_abstract` 步骤）。

### Step 2: 占位符替换

扫描全文中所有占位符，按以下规则替换：

#### 2a. Table/Figure 全局编号（D-12）

Table 和 Figure 各自独立编号，按 IMRAD 段顺序扫描：

```
table_counter = 1
figure_counter = 1

for section in [introduction, methods, results, discussion]:
    content = sections[section]
    
    # 替换 Table 占位符
    while content contains {{Table:\d+}}:
        replace {{Table:N}} → Table {table_counter}
        table_counter += 1
    
    # 替换 Figure 占位符
    while content contains {{Figure:\d+}}:
        replace {{Figure:N}} → Figure {figure_counter}
        figure_counter += 1
```

**示例**:
- Methods 段 `{{Table:1}}` → 全文第 1 个 Table → `Table 1`
- Results 段 `{{Table:1}}` → 全文第 2 个 Table（Methods 已有 Table 1）→ `Table 2`
- Results 段 `{{Figure:1}}` → 全文第 1 个 Figure → `Figure 1`

#### 2b. Supplementary Table/Figure 编号

与 Table/Figure 一样独立编号，按出现顺序：
```
supp_table_counter = 1
supp_figure_counter = 1
```
处理方式同主 Table/Figure。

#### 2c. Method 引用替换

```
方法引用映射:
- {{Method:BaselineTable}} → "the baseline characteristics analysis (as described in Methods)"
- {{Method:TwoGroupComparison}} → "the two-group comparison analysis"
- {{Method:SurvivalAnalysis}} → "the survival analysis"
- {{Method:CorrelationAnalysis}} → "the correlation analysis"
- {{Method:SubgroupAnalysis}} → "the subgroup analysis"

默认规则: {{Method:CamelCaseName}} → "the {Name in lowercase} analysis"
```

从 `project_config.yml` 的 `analysis_plan.waves[*].methods[*].id` 读取分析方法名列表，建立映射。

#### 2d. Section 交叉引用替换

```
{{Section:methods}} → "Methods"
{{Section:results}} → "Results"
{{Section:introduction}} → "Introduction"
{{Section:discussion}} → "Discussion"
```

### Step 3: 引用统一编号

引用重编号是拼接的核心——这是 WRITE-02 的"引用在合并时统一整理"。

#### 3a. 扫描全文收集引用

```python
# 扫描正文中所有 [id] 引用标记，如 [1], [2,3], [1-3], [1,4,7]
# 提取所有 unique 的引用 ID
# 从 Reference/reference_library.json 读取每个 ID 对应的引用条目

citation_map = {}  # old_id -> new_id (sequential)

# 按正文出现顺序收集所有引用 ID
all_ids = []
for match in regex_find_all(/\[(\d+(?:[-,]\d+)*)\]/):
    ids = parse_id_group(match)  # [1,4,7] → [1,4,7], [1-3] → [1,2,3]
    for id in ids:
        if id not in all_ids:
            all_ids.append(id)

# 分配连续编号
for i, old_id in enumerate(all_ids):
    citation_map[old_id] = i + 1  # [1] 开始
```

#### 3b. 替换正文中所有引用编号

```python
for each [old_id] in manuscript:
    new_id = citation_map[old_id]
    replace [old_id] → [new_id]
    
# 处理组合: [1,2,3] → 如果新编号分别为 [4,5,6]
# 组合编号在替换后重新格式化，保持原有分组
# [1-3] → [new(1)-new(3)], [1,4,7] → [new(1),new(4),new(7)]
```

#### 3c. 生成 References 区

```python
# 按新编号顺序生成 References 列表
references_section = "## References\n\n"

for new_id in 1..len(all_ids):
    old_id = reverse_map[new_id]
    entry = library.references[old_id]
    
    # Vancouver 格式: [N] Authors. Title. Journal. Year;Vol(Issue):Pages. doi:DOI
    authors = format_authors(entry.authors)  # "Author A, Author B, et al."
    ref_text = f"[{new_id}] {authors}. {entry.title}. {entry.journal}. {entry.year}"
    if entry.volume:
        ref_text += f";{entry.volume}"
        if entry.issue:
            ref_text += f"({entry.issue})"
    if entry.pages:
        ref_text += f":{entry.pages}"
    ref_text += f". doi:{entry.doi}\n"
    
    references_section += ref_text
```

#### 3d. 去重处理（D-07, D-09）

同一引用在多个段落出现使用同一新编号，自然完成去重。`reference_library.json` 已经保证了库级别的去重（citation_key 主键），拼接时只做编号重排以保持连续性。

### Step 4: 生成 YAML Frontmatter（D-16）

```yaml
---
title: ""  # 由用户在最终审阅时填写
target_journal: ""  # 从 project_config.yml 读取
word_count: 0  # 由协议自动计算（正文 + 参考文献字数）
reference_count: 0  # 引用条目总数
---
```

计算方式：
- `target_journal` → `project_config.yml` 的 `target_journal` 字段
- `word_count` → 统计合并后正文（不含 YAML frontmatter 和 References 部分）的中文字符 + 英文单词数
- `reference_count` → 最终 References 区的条目总数

### Step 5: 合并输出

最终 manuscript.md 结构：

```markdown
---
title: ""
target_journal: ""
word_count: 0
reference_count: 0
---

# {Title from project_config.yml 或留空}

## Introduction

{Introduction 段内容，引用已重编号为 [1] 开始}

## Methods

{Methods 段内容}

## Results

{Results 段内容}

## Discussion

{Discussion 段内容}

## References

[1] Author A, ...  (Vancouver 格式)
[2] Author B, ...
...
```

### Step 6: 更新 MANIFEST.yaml（D-15）

在 `05_Manuscript/MANIFEST.yaml` 写入（参考 manifest-format.md）：

```yaml
agent: writer-agent
phase: 3
type: manuscript
outputs:
  - directory: 05_Manuscript/
    files:
      - name: manuscript.md
        format: md
      - name: sections/01-introduction.md
        format: md
      - name: sections/02-methods.md
        format: md
      - name: sections/03-results.md
        format: md
      - name: sections/04-discussion.md
        format: md
handoffs:
  - consumer: clinpub-verifier
    required_files:
      - 05_Manuscript/manuscript.md
    required_quality:
      - "all citations have DOIs"
      - "IMRAD structure complete (5 sections: I/M/R/D + References)"
      - "word_count > 5000"
      - "reference_count >= 20"
```

### Step 7: 更新引用库

拼接后更新 `Reference/reference_library.json`：
- 追加 `"concatenated": true` 标记
- 更新 `"last_updated"` 时间戳
- 可选的 `"final_reference_count"` 字段记录最终引用数

---

## 验证检查清单

拼接完成后验证：

- [ ] 各段内容完整引入，无段落遗漏
- [ ] `{{Table:\d+}}` 和 `{{Figure:\d+}}` 全部替换，无残留占位符（regex scan 确认）
- [ ] `{{Method:\w+}}` 全部替换
- [ ] Table/Figure 编号连续无跳跃（1,2,3... 非 1,3,5）
- [ ] 引用编号从 [1] 开始连续
- [ ] References 区每个条目对应正文中出现的引用
- [ ] YAML frontmatter 字段非空
- [ ] word_count > 5000
- [ ] reference_count 正确
- [ ] MANIFEST.yaml 声明了所有输出文件
- [ ] 05_Manuscript/sections/ 下各段文件保留
```
</action>
<acceptance_criteria>
- grep -c "### Step 1\|### Step 2\|### Step 3\|### Step 4\|### Step 5\|### Step 6\|### Step 7" pipeline/references/concatenation-protocol.md 返回 7（七个步骤）
- grep -c "{{Table:\|{{Figure:\|{{Method:\|{{Section:" pipeline/references/concatenation-protocol.md 返回 >= 3（占位符替换）
- grep -c "reference_library.json" pipeline/references/concatenation-protocol.md 返回 >= 3（引用库读取 + 重编号）
- grep -c "YAML frontmatter\|frontmatter" pipeline/references/concatenation-protocol.md 返回 >= 2（YAML frontmatter 生成）
- grep -c "MANIFEST.yaml" pipeline/references/concatenation-protocol.md 返回 >= 2（manifest 生成）
- grep -c "word_count\|reference_count" pipeline/references/concatenation-protocol.md 返回 >= 2（元数据计算）
- grep -c "D-07\|D-08\|D-09\|D-11\|D-12\|D-14\|D-15\|D-16" pipeline/references/concatenation-protocol.md 返回 >= 5（决策引用）
- grep -c "Vancouver" pipeline/references/concatenation-protocol.md 返回 >= 2（引用格式）
- grep -c "reference-library.md" pipeline/references/concatenation-protocol.md 返回 >= 1（引用库规范引用）
- grep -c "05_Manuscript/sections/" pipeline/references/concatenation-protocol.md 返回 >= 2（各段文件引用 + 保留）
- grep -c "拼接而非重写\|非重写\|not rewrite\|WRITE-02" pipeline/references/concatenation-protocol.md 返回 >= 1（WRITE-02 原则声明）
</acceptance_criteria>
<verify>
  <automated>grep -c "### Step 1\|### Step 2\|### Step 3\|### Step 4\|### Step 5\|### Step 6\|### Step 7" pipeline/references/concatenation-protocol.md</automated>
  <automated>grep -c "reference_library.json" pipeline/references/concatenation-protocol.md</automated>
  <automated>grep -c "YAML frontmatter" pipeline/references/concatenation-protocol.md</automated>
  <automated>grep -c "MANIFEST.yaml" pipeline/references/concatenation-protocol.md</automated>
  <automated>grep -c "D-07\|D-08\|D-09\|D-11\|D-12\|D-14\|D-15\|D-16" pipeline/references/concatenation-protocol.md</automated>
</verify>
<done>concatenation-protocol.md 创建完成：包含 7 步骤拼接协议（段落合并、占位符替换、引用统一编号、YAML frontmatter 生成、合并输出、MANIFEST.yaml 更新、引用库更新）+ 验证清单</done>
</task>

<task type="auto">
<name>Task 2: 更新 workflow — 替换占位符步骤为真实拼接步骤</name>
<files>pipeline/workflows/writing.md</files>
<read_first>
- `pipeline/workflows/writing.md` — Plan 03-01 修改后的工作流（当前有 `placeholder_concatenation` 占位符步骤和 `verify_manuscript` 步骤）
- `pipeline/references/concatenation-protocol.md` — Task 1 创建的拼接协议
- `.clinpub/phases/03-手稿拼接/03-CONTEXT.md` — D-14~D-16
</read_first>
<action>
修改 `pipeline/workflows/writing.md`，将 `placeholder_concatenation` 步骤替换为真实的拼接步骤：

**a) 删除 `placeholder_concatenation` 步骤，替换为 `concatenate_manuscript` 步骤**：

找到 `placeholder_concatenation` 步骤（内容类似 "注意：终稿拼接在 Plan 03-03 中处理..."），删除并替换为：

```markdown
<step name="concatenate_manuscript" priority="high">
执行终稿拼接协议（Concatenation Protocol）将各段合并为最终 manuscript.md。

按 `@./pipeline/references/concatenation-protocol.md` 执行以下步骤：

1. **段落合并**: 按 IMRAD 顺序读取 `05_Manuscript/sections/01-introduction.md` ~ `04-discussion.md`
2. **占位符替换**:
   - `{{Table:N}}` → 按 IMRAD 顺序全局编号（Table 1, Table 2...）
   - `{{Figure:N}}` → 按 IMRAD 顺序全局编号（Figure 1, Figure 2...）
   - `{{SupplementaryTable:N}}` → 独立编号
   - `{{SupplementaryFigure:N}}` → 独立编号
   - `{{Method:name}}` → 替换为 "the {name} analysis"
   - `{{Section:name}}` → 替换为段落名
3. **引用统一编号**:
   - 读取 `Reference/reference_library.json`
   - 扫描全文中所有 `[id]` 引用
   - 按正文出现顺序重新分配连续编号 [1] 开始
   - 同一引用在多段使用自动复用编号（自然去重）
   - 在文末生成统一的 References 区（Vancouver 格式）
4. **YAML frontmatter 生成**:
   - `title`: 留空（由用户在最终审阅填写）或从 project_config.yml project.title 读取
   - `target_journal`: 从 project_config.yml 读取
   - `word_count`: 自动计算正文（中文 + 英文单词数）
   - `reference_count`: 引用条目总数
5. **写入 manuscript.md**:
   ```markdown
   ---
   title: "{title}"
   target_journal: "{journal}"
   word_count: {count}
   reference_count: {count}
   ---
   
   # {Title}
   
   ## Introduction
   ...
   
   ## Methods
   ...
   
   ## Results
   ...
   
   ## Discussion
   ...
   
   ## References
   [1] ...
   [2] ...
   ```
6. **更新 MANIFEST.yaml**: 写入 `05_Manuscript/MANIFEST.yaml`（声明 manuscript.md + sections/ 下所有文件，consumer 为 clinpub-verifier）
7. **更新引用库**: 追加 `concatenated: true` 标记，更新时间戳

验证（执行后检查）：
- 扫描全文确认无 `{{Table:\d+}}` 或 `{{Figure:\d+}}` 残留
- word_count > 5000
- reference_count >= 20
- 所有引用有 DOI
- IMARD 结构完整

参见 `@./pipeline/references/concatenation-protocol.md` 获取各步骤的详细伪代码和规则。
</step>
```

**b) 在 `concatenate_manuscript` 步骤后添加输出提示**：

```markdown
<step name="concatenation_output" priority="medium">
拼接完成后输出：

```
✅ 终稿拼接完成

文件:
- 05_Manuscript/manuscript.md — 完整终稿（{word_count} 字, {reference_count} 篇引用）
- 05_Manuscript/sections/ — 各段独立文件

下一步:
- 检查 manuscript.md 确认拼接质量
- 如需要调整 → 直接编辑 manuscript.md（非重写原则下，手动修正局部问题）
- 如确认无误 → 进入最终 checkpoint_confirm
```
</step>
```

**c) 更新 workflow 的 `<success_criteria>`**，追加拼接完成标准：

在 Plan 03-01 的 success_criteria 基础上追加：
- 05_Manuscript/manuscript.md 存在，包含 YAML frontmatter 和完整 IMRAD 结构
- 05_Manuscript/sections/ 下 4 个段文件保留
- 全文中无残留占位符
- 引用从 [1] 开始连续编号，文末 References 区完整
- word_count > 5000, reference_count >= 20
- MANIFEST.yaml 存在且声明所有输出
</action>
<acceptance_criteria>
- grep -c "concatenate_manuscript" pipeline/workflows/writing.md 返回 1（新拼接步骤）
- grep -c "concatenation-protocol.md" pipeline/workflows/writing.md 返回 >= 2（拼接协议引用）
- grep -c "05_Manuscript/manuscript.md" pipeline/workflows/writing.md 返回 >= 2（终稿输出）
- grep -c "05_Manuscript/sections/" pipeline/workflows/writing.md 返回 >= 2（各段文件保留）
- grep -c "MANIFEST.yaml" pipeline/workflows/writing.md 返回 >= 1（manifest 更新）
- grep -c "word_count\|reference_count" pipeline/workflows/writing.md 返回 >= 1（元数据）
- 命令 grep -c "placeholder_concatenation" pipeline/workflows/writing.md 返回 0（占位符步骤被移除）
- grep -c "concatenation_output" pipeline/workflows/writing.md 返回 1（输出提示步骤）
</acceptance_criteria>
<verify>
  <automated>grep -c "concatenate_manuscript" pipeline/workflows/writing.md</automated>
  <automated>grep -c "concatenation-protocol.md" pipeline/workflows/writing.md</automated>
  <automated>grep -c "05_Manuscript/manuscript.md" pipeline/workflows/writing.md</automated>
  <automated>grep -c "MANIFEST.yaml" pipeline/workflows/writing.md</automated>
</verify>
<done>writing.md 拼接步骤替换完成：placeholder_concatenation → concatenate_manuscript，包含 7 步拼接逻辑、concatenation-protocol.md 引用、输出提示、更新 success_criteria</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 05_Manuscript/sections/ → concatenation | 各段文件作为拼接输入源 |
| Reference/reference_library.json → concatenation | 引用库决定最终引用编号和 References 区 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | 占位符替换错误（如两个不同 Table 被错误编为同一编号） | mitigate | 占位符替换严格按 IMRAD 段顺序扫描，table_counter 和 figure_counter 独立递增。每个 {{Table:N}} 唯一匹配一个 Table 编号 |
| T-03-09 | DoS | 引用重编号时丢失或重复引用 | mitigate | 三步法：收集所有 unique ID → 按出现顺序分配新编号 → 全文扫描替换。解析组合编号（[1-3], [1,4,7]）时正确展开 |
| T-03-10 | Spoofing | sections/ 目录中某段文件缺失导致拼接不完整 | mitigate | 拼接开始时验证所有 4 个段文件存在，缺失则报错并停止，不自动跳过 |
| T-03-11 | Tampering | word_count 或 reference_count 计算错误 | mitigate | word_count 使用简单的字符统计规则（中文字符数 + 英文单词数）。reference_count 直接从最终 References 区条数获取。建议手动复查 |
</threat_model>

<verification>
1. `pipeline/references/concatenation-protocol.md` 存在且包含 7 个步骤
2. 占位符替换覆盖 Table/Figure/Method/Section 四种类型
3. 引用统一编号包含收集、重排、替换三阶段
4. YAML frontmatter 字段定义完整
5. MANIFEST.yaml 生成格式正确
6. `pipeline/workflows/writing.md` 中 `concatenate_manuscript` 步骤存在
7. `placeholder_concatenation` 步骤被移除
8. workflow 的 success_criteria 已更新
9. 拼接完成后的验证检查清单存在
</verification>

<success_criteria>
- [ ] `grep -c "concatenate_manuscript" pipeline/workflows/writing.md` >= 1
- [ ] `grep -c "concatenation-protocol.md" pipeline/workflows/writing.md` >= 2
- [ ] `grep -c "### Step 1\|### Step 2\|### Step 3\|### Step 4\|### Step 5\|### Step 6\|### Step 7" pipeline/references/concatenation-protocol.md` = 7
- [ ] `grep -c "YAML frontmatter" pipeline/references/concatenation-protocol.md` >= 2
- [ ] `grep -c "reference_library.json" pipeline/references/concatenation-protocol.md` >= 3
- [ ] `grep -c "D-07\|D-08\|D-09\|D-11\|D-12\|D-14\|D-15\|D-16" pipeline/references/concatenation-protocol.md` >= 5
- [ ] `grep -c "MANIFEST.yaml" pipeline/references/concatenation-protocol.md` >= 2
- [ ] `grep -c "MANIFEST.yaml" pipeline/workflows/writing.md` >= 1
- [ ] MANIFEST.yaml 格式符合 manifest-format.md 规范
- [ ] YAML frontmatter 包含 title, target_journal, word_count, reference_count
</success_criteria>

<output>
After completion, create `.clinpub/phases/03-手稿拼接/03-03-SUMMARY.md`
</output>
