---
phase: 03-手稿拼接
plan: 04
type: execute
wave: 3
depends_on: [03-01]
files_modified:
  - commands/clinpub/writing.md
autonomous: true
requirements: [WRITE-01]

must_haves:
  truths:
    - "命令入口描述反映逐段撰写的分段流程"
    - "命令入口引用更新后的工作流文件和引用管理规范"
  artifacts:
    - path: "commands/clinpub/writing.md"
      provides: "Updated command entry with segmented writing flow reference"
      contains: "clinpub:writing"
  key_links:
    - from: "commands/clinpub/writing.md"
      to: "pipeline/workflows/writing.md"
      via: "入口引用更新后的工作流"
      pattern: "pipeline/workflows/writing.md"
    - from: "commands/clinpub/writing.md"
      to: "pipeline/references/reference-library.md"
      via: "入口引用共享引用库规范"
      pattern: "reference-library.md"
---

<objective>
更新 `commands/clinpub/writing.md` 命令入口，使其符合 Phase 3 分段撰写新流程。

Purpose: 命令入口是用户执行 `/clinpub-writing` 时看到的顶层描述。当前入口描述的是单次全篇撰写（"draft a complete IMRAD manuscript"）。需要更新为逐段顺序撰写的描述，并引用新的规范化引用库文档。

Output:
- `commands/clinpub/writing.md` — 更新后的命令入口
</objective>

<execution_context>
@./commands/clinpub/writing.md
@./pipeline/workflows/writing.md
@./pipeline/references/reference-library.md
@./.clinpub/phases/03-手稿拼接/03-CONTEXT.md
</execution_context>

<context>
@./commands/clinpub/writing.md

<!-- 关键决策 (from 03-CONTEXT.md) -->
<!-- D-01: IMRAD 顺序流水线 (Intro → Methods → Results → Discussion), pause 审阅 -->
<!-- D-02: 同一 writer-agent, 4 轮对话 -->
<!-- D-06: 每段前 reference-agent 搜索文献 -->
<!-- D-07~D-09: 共享引用库，Vancouver 格式，全局统一编号 -->
</context>

<tasks>

<task type="auto">
<name>Task 1: 更新命令入口 frontmatter, objective, execution_context</name>
<files>commands/clinpub/writing.md</files>
<read_first>
- `commands/clinpub/writing.md` — 当前命令入口（36 行），理解其格式和内容
- `.clinpub/phases/03-手稿拼接/03-CONTEXT.md` — D-01~D-16 全部决策
- `pipeline/workflows/writing.md` — Plan 03-01 修改后的工作流
- `pipeline/references/reference-library.md` — Plan 03-02 创建的引用管理规范
</read_first>
<action>
分部分修改 `commands/clinpub/writing.md`：

**a) 更新 frontmatter `description`**：

当前（第 3 行）：
```yaml
description: "Phase 3: IMRAD manuscript writing. Reference Agent performs literature search, Writer Agent follows study type template to draft full IMRAD manuscript in Chinese with English figures/tables. Vancouver citations with DOIs."
```

替换为：
```yaml
description: "Phase 3: IMRAD sequential manuscript writing. Sections written one-by-one (Introduction → Methods → Results → Discussion) with reference-agent pre-search before each section, writer-agent per-section drafting, and user review pause between sections. Shared reference library for deduplication. Placeholder-based cross-references. Final concatenation produces manuscript.md with YAML frontmatter."
```

**b) 更新 `<objective>`**：

当前（第 14-18 行）：
```markdown
<objective>
Phase 3: Manuscript writing. Draft a complete IMRAD manuscript in Chinese (English figures/tables) targeting SCI Q1/Q2 journals.

Two-agent workflow: Reference Agent (literature search) → Writer Agent (manuscript drafting). Follows the appropriate research type template (RCT/cohort/case-control/cross-sectional/descriptive).
</objective>
```

替换为：
```markdown
<objective>
Phase 3: Sequential IMRAD manuscript writing with per-section review.

## 流程概述

四段（Introduction → Methods → Results → Discussion）按顺序独立撰写引用后拼接为终稿。每段三段式：Reference Agent 文献预搜索 → Writer Agent 撰写 → 用户审阅 pause，确认后再进入下一段。

## 核心约束

- **D-01 顺序**: IMRAD 顺序（Intro → Methods → Results → Discussion），每段写完 pause 用户审阅
- **D-02 Agent 模式**: 同一个 writer-agent，4 轮对话分别写 4 段，不扩展 agent role 定义
- **D-03 上下文**: 自动读取项目数据（spec → Methods, analysis outputs → Results, 文献 → Intro/Discussion）
- **D-04 篇幅**: 全文 >5000 字，自然成段论述（非 bullet point）
- **D-05 Methods**: 从 spec + analysis pipeline outputs 自动生成初稿
- **D-06 文献搜索**: 每段前 reference-agent 搜索文献，更新共享引用库
- **D-09 引用库**: 全局统一编号，共享引用库（Reference/reference_library.json），写各段时查询已有引用
- **D-11 占位符**: 使用 {{Table:N}} {{Figure:N}} {{Method:name}} {{Section:name}} 进行交叉引用
- **D-15 输出**: 05_Manuscript/manuscript.md 最终稿 + 05_Manuscript/sections/ 各段独立文件
</objective>
```

**c) 更新 `<execution_context>`**，追加引用库规范：

当前（第 20-22 行）：
```markdown
<execution_context>
@./pipeline/workflows/writing.md
@./pipeline/references/journal_standards.md
</execution_context>
```

替换为：
```markdown
<execution_context>
@./pipeline/workflows/writing.md
@./pipeline/references/journal_standards.md
@./pipeline/references/reference-library.md
@./pipeline/contexts/writing.md
</execution_context>
```

**d) 更新 `<success_criteria>`**，反映分段完成标准：

当前（第 29-35 行）：
```markdown
<success_criteria>
- IMRAD structure complete (Methods → Results → Introduction → Discussion → Abstract)
- All citations have DOIs
- All figures/tables referenced in text
- STROBE/CONSORT checklist covered
- Language consistent (Chinese manuscript, English figures/tables)
- No AI-template patterns (checked via Humanizer checklist)
</success_criteria>
```

替换为：
```markdown
<success_criteria>
- 四个 IMRAD 段按顺序独立完成撰写（Intro → Methods → Results → Discussion）
- 每段撰写前 reference-agent 完成文献搜索，引用库不重复
- 每段撰写后用户完成审阅确认后才进入下一段
- 各段写入 05_Manuscript/sections/ 独立文件
- 各段使用占位符进行交叉引用（{{Table:N}} {{Figure:N}} {{Method:name}}）
- 各段引用通过共享引用库管理，不重复
- 所有引用有 DOI
- 全文 >5000 字，自然成段论述
- 无 AI-template 模式（Humanizer 自检通过）
</success_criteria>
```
</action>
<acceptance_criteria>
- grep -c "description: \"Phase 3: IMRAD sequential manuscript writing" commands/clinpub/writing.md 返回 1（更新后的前端描述）
- grep -c "Sequential IMRAD manuscript writing" commands/clinpub/writing.md 返回 1（新 objective）
- grep -c "D-01\|D-02\|D-03\|D-04\|D-05\|D-06\|D-09\|D-11\|D-15" commands/clinpub/writing.md 返回 >= 7（决策引用）
- grep -c "reference-library.md" commands/clinpub/writing.md 返回 1（新增引用管理规范引用）
- grep -c "pipeline/contexts/writing.md" commands/clinpub/writing.md 返回 1（新增上下文配置引用）
- grep -c "05_Manuscript/sections/" commands/clinpub/writing.md 返回 >= 1（各段输出目录）
- grep -c "用户审阅\|pause" commands/clinpub/writing.md 返回 >= 1（逐段审阅）
- grep -c "{{Table:\|{{Figure:\|{{Method:" commands/clinpub/writing.md 返回 >= 2（占位符引用）
- grep -c "Humanizer" commands/clinpub/writing.md 返回 >= 1（Humanizer 自检保留）
</acceptance_criteria>
<verify>
  <automated>grep -c "reference-library.md" commands/clinpub/writing.md</automated>
  <automated>grep -c "D-01\|D-02" commands/clinpub/writing.md</automated>
  <automated>grep -c "05_Manuscript/sections/" commands/clinpub/writing.md</automated>
  <automated>grep -c "{{Table:\|{{Figure:" commands/clinpub/writing.md</automated>
</verify>
<done>commands/clinpub/writing.md 命令入口更新完成：反映逐段顺序撰写流程，追加引用库和上下文配置引用，更新 objective 和 success_criteria</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writing.md → user | 命令入口描述影响用户对流程的理解 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | DoS | 命令入口描述与工作流实际行为不一致 | mitigate | 入口引用工作流本身（@./pipeline/workflows/writing.md），用户执行时 workflow 是真实行为来源。入口只做摘要描述 |

</threat_model>

<verification>
1. `commands/clinpub/writing.md` 的 frontmatter description 更新为分段撰写描述
2. objective 包含所有关键决策引用和流程约束
3. execution_context 追加 reference-library.md 和 writing context
4. success_criteria 反映分段完成标准
5. 所有 D 决策引用符合 CONTEXT.md 锁定决策
</verification>

<success_criteria>
- [ ] `grep -c "reference-library.md" commands/clinpub/writing.md` >= 1
- [ ] `grep -c "D-01\|D-02\|D-03\|D-04\|D-05\|D-06\|D-09\|D-11\|D-15" commands/clinpub/writing.md` >= 7
- [ ] `grep -c "05_Manuscript/sections/" commands/clinpub/writing.md` >= 1
- [ ] `grep -c "user review pause\|用户审阅\|pause" commands/clinpub/writing.md` >= 1
- [ ] `grep -c "{{Table:\|{{Figure:\|{{Method:" commands/clinpub/writing.md` >= 2
</success_criteria>

<output>
After completion, create `.clinpub/phases/03-手稿拼接/03-04-SUMMARY.md`
</output>
