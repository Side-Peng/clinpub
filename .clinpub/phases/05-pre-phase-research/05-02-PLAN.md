---
phase: 05-pre-phase-research
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/reference-agent.md
autonomous: true
requirements:
  - FLOW-01
must_haves:
  truths:
    - "reference-agent 可以执行 Track A 领域调研（调用 PubMed + Tavily 搜索学术内容）"
    - "reference-agent 可以执行 Track B 技术调研（读取 codebase 地图 + Tavily 搜索技术方案）"
    - "phase_research 模式输出符合 RESEARCH.md 格式要求"
    - "phase_research 模式支持自适应深度（摘要级首轮，追问后深入）"
    - "phase_research 模式复用现有搜索基础设施（不新写搜索脚本）"
  artifacts:
    - path: "agents/reference-agent.md"
      provides: "Reference agent with phase_research mode"
      min_lines: 10
      contains: "phase_research|Phase Research"
  key_links:
    - from: "agents/reference-agent.md (phase_research mode)"
      to: "scripts/pubmed_search.py"
      via: "Track A: academic literature search"
      pattern: "pubmed_search"
    - from: "agents/reference-agent.md (phase_research mode)"
      to: "scripts/tavily_search.py"
      via: "Track A + Track B: supplementary / technical search"
      pattern: "tavily_search"
    - from: "agents/reference-agent.md (phase_research mode)"
      to: ".clinpub/codebase/*.md"
      via: "Track B: codebase map reading"
      pattern: "codebase"
    - from: "agents/reference-agent.md"
      to: "pipeline/references/pre-phase-research.md"
      via: "phase_research mode is the execution tool for the pre-phase-research workflow"
      pattern: "pre-phase-research"
---

<objective>
为 reference-agent 添加 `phase_research` 模式，使其能够执行 Phase 前调研（Track A 领域调研 + Track B 技术调研），产出标准化 RESEARCH.md。

Purpose: 按照 Phase 4 已验证的 agent 模式扩展模式（`method_search` 的 precedent），为 reference-agent 增加执行 Phase 前调研的能力。调研结果供下游 discuss-phase 使用。

Output: `agents/reference-agent.md` — 新增 phase_research 步骤
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.clinpub/PROJECT.md
@.clinpub/ROADMAP.md
@.clinpub/STATE.md

# Phase 5 context (D-01 through D-12)
@.clinpub/phases/05-pre-phase-research/05-CONTEXT.md

# Reference agent — must follow existing method_search pattern
@agents/reference-agent.md

# The workflow document this mode serves
@pipeline/references/pre-phase-research.md

# Existing search scripts to call
@scripts/pubmed_search.py
@scripts/tavily_search.py
</context>

<tasks>

<task type="auto">
<name>Task 1: 添加 phase_research 步骤定义到 reference-agent.md</name>
<files>agents/reference-agent.md</files>
<read_first>
- agents/reference-agent.md (全文，理解现有 step 结构、method_search 模式格式)
- .clinpub/phases/05-pre-phase-research/05-CONTEXT.md (D-01, D-02, D-03, D-04, D-09, D-10)
</read_first>
<action>
在 `agents/reference-agent.md` 的 `<execution_flow>` 中，在现有 `<step name="method_search">` 之后、`<step name="full_text_retrieval">` 之前，插入一个新的步骤：

```xml
<step name="phase_research" priority="high">
Phase 前调研模式。在 GSD discuss-phase 之前执行，为每个 clinpub 开发 Phase（6、7 及后续）提供领域/技术背景调研。

**触发条件：**
- 在 GSD plan-phase 之前，planner agent 读取 `pipeline/references/pre-phase-research.md` 后触发
- 或用户直接要求："调研一下 Phase N 的领域/技术背景"
- 首次触发时不会自动展开，输出摘要级发现

**轨道选择（per D-01 双轨制）：**

根据 Phase 名称和 ROADMAP.md 描述判断轨道：

Track A 关键词（领域调研）：分析、方法、文献、引用、统计、研究、临床、期刊
Track B 关键词（技术调研）：命令、钩子、workflow、配置、工具、重构、脚本、文档

匹配规则：
- 只匹配 Track A 关键词 → Track A
- 只匹配 Track B 关键词 → Track B
- 两者都有 → 双轨（先 A 后 B）
- 两者都没有 → 默认 Track A（领域优先）

**轨道执行：**

### Track A — 领域调研

调研目标：搜索临床/方法学领域的最新文献、最佳实践、研究空白。

搜索策略：
1. 从 ROADMAP.md 的 Phase Goal 和 Requirements 提取核心关键词
2. PubMed 学术搜索：
   ```bash
   python scripts/pubmed_search.py "{phase_keywords} clinical research" --max 5 --type review
   ```
3. Tavily 补充搜索：
   ```bash
   python scripts/tavily_search.py "{phase_description} clinical practice guideline" --depth basic --max-results 5
   ```

自适应深度（per D-02）：
- **第一轮 — 摘要级**：产出一句话概述 + 3-5 个关键发现 + 2-3 篇参考文献。控制在够做决策的量级。
- **用户追问时 — 深入层**：展开原理说明、提供实现细节或代码示例、补充更多参考文献。
- 无追问时不得自动展开。

### Track B — 技术调研

调研目标：搜索工程技术方案、库选型、实现策略。

搜索策略：
1. 读取 codebase 地图文件：
   - 读取 `.clinpub/codebase/ARCHITECTURE.md`（架构）、`CONVENTIONS.md`（规范）、`STRUCTURE.md`（结构）、`INTEGRATIONS.md`（集成）
   - 这些文件已在 `.clinpub/codebase/` 下存在（per D-10），无需额外扫描逻辑
2. Tavily 技术搜索：
   ```bash
   python scripts/tavily_search.py "{technology_keyword} implementation guide best practice" --depth advanced --max-results 5
   ```
3. 库选型对比（如涉及）：
   ```bash
   python scripts/tavily_search.py "{lib_a} vs {lib_b} comparison features" --depth advanced --max-results 3
   ```

自适应深度（同 Track A）：首轮摘要，追问后展开。

### 双轨模式

当 Phase 同时需要领域和技术调研时：
1. 先执行 Track A（领域优先）
2. 再执行 Track B
3. 输出合并为一个统一的调研报告

**输出要求：**
- 调研结果写入该 Phase 目录下的 RESEARCH.md（如 `.clinpub/phases/06-引用策略/06-RESEARCH.md`）（per D-03）
- 不覆盖同目录下已有文件
- 如果 RESEARCH.md 已存在，输出调研发现后与用户确认是否更新

**搜索注意事项：**
- 复用全部现有搜索基础设施（per D-09），不创建新脚本
- 检查 TAVILY_API_KEY 和 NCBI_API_KEY 是否设置（复用 existing `<step name="check_api_keys">`）
- 参考文献必须有真实 DOI，禁止编造（继承 reference-agent 的核心规则）
- 如果搜索无结果，报告"未找到相关文献"而非编造
- Tavily 搜索优先于 PubMed（更快获取概述），PubMed 用于补充学术深度
</step>
```

**插入位置说明：**
- 在 `method_search` 步骤的 `</step>` 关闭标签之后、`full_text_retrieval` 的 `<step name="full_text_retrieval">` 之前插入
- 即在 `<execution_flow>` 中 method_search 之后、full_text_retrieval 之前

</action>
<verify>
<automated>grep -c "phase_research\|Phase Research" agents/reference-agent.md && grep -c "Track A\|Track B\|双轨" agents/reference-agent.md && grep -c "pubmed_search\|tavily_search" agents/reference-agent.md && grep -c "codebase.*ARCHITECTURE\|codebase.*STRUCTURE" agents/reference-agent.md</automated>
</verify>
<done>
phase_research step 插入到 execution_flow 中，位置正确（method_search 之后），包含 Track A/B 搜索策略、自适应深度规则、输出要求
</done>
</task>

<task type="auto">
<name>Task 2: 在 method_search 的 critical_rules 和 success_criteria 后追加 phase_research 规则和标准</name>
<files>agents/reference-agent.md</files>
<read_first>
- agents/reference-agent.md (Task 1 修改后的完整文件，特别是末尾的 `<critical_rules>` 和 `<success_criteria>` 区域)
</read_first>
<action>
在 `agents/reference-agent.md` 的 `<critical_rules>` 节中，在 `### method_search 规则` 之后追加：

```markdown
### phase_research 规则
- phase_research 必须先判断轨道（Track A/B/双轨），再执行对应搜索策略（D-01）
- 首轮输出必须控制在摘要级（3-5 个关键点 + 2-3 篇参考）（D-02）
- 用户追问后才展开深入层，禁止首轮全量输出
- 搜索结果写入 `{phase_dir}/{phase}-RESEARCH.md`（D-03）
- 不创建新搜索脚本，复用全部现有搜索基础设施（D-09）
- 不修改 GSD 框架文件，只操作 clinpub 产品层文件（D-11）
- codebase 扫描使用 `.clinpub/codebase/*.md` 现有地图文件，不写额外扫描逻辑（D-10）
- Track A 必须同时搜索 PubMed 和 Tavily（PubMed 提供学术深度，Tavily 提供概述）
- 如果 TAVILY_API_KEY 未设置，Track B 搜索不可用时报告用户并提供设置指引
```

在 `agents/reference-agent.md` 的 `<success_criteria>` 节中，在 `### method_search 标准` 之后追加：

```markdown
### phase_research 标准
- phase_research 输出符合 D-04 RESEARCH.md 标准结构（主题与范围 + 关键发现 + 可选方案对比 + 参考来源 + 建议下游操作）
- Track A 搜索同时覆盖 PubMed 和 Tavily
- Track B 搜索先读 codebase 地图再 Tavily
- 输出深度控制在自适应层（摘要级首轮，深入层仅当追问时）
- 所有引用有真实 DOI/URL（如搜索不到则注明）
```
</action>
<verify>
<automated>grep -c "phase_research 规则\|phase_research 标准" agents/reference-agent.md</automated>
</verify>
<done>
critical_rules 和 success_criteria 都追加了 phase_research 专属条款
</done>
</task>

</tasks>

<threat_model>
Phase 5 只修改现有 agent 文档，不涉及用户数据或外部 API 调用变更。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase_research 输出→ discuss-phase 输入 | 调研结果可能影响 Phase 方向决策 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T (Tampering) | phase_research 输出内容 | accept | RESEARCH.md 仅为参考输入，用户通过 discuss-phase 做最终决策 |
| T-05-04 | E (Elevation of Privilege) | phase_research mode 执行搜索 | accept | 仅调用现有搜索脚本，无新增权限 |
</threat_model>

<verification>
1. `agents/reference-agent.md` 包含 `phase_research` 步骤
2. 步骤包含 Track A 和 Track B 的搜索协议
3. 步骤包含自适应深度规则
4. critical_rules 和 success_criteria 都追加了 phase_research 相关条目
5. 步骤位置正确（method_search 之后，full_text_retrieval 之前）
</verification>

<success_criteria>
1. reference-agent.md 包含 phase_research 步骤定义
2. phase_research 步骤包含 Track A（领域调研）搜索协议
3. phase_research 步骤包含 Track B（技术调研）搜索协议
4. phase_research 步骤包含双轨合并规则
5. phase_research 步骤包含自适应深度规则
6. `<critical_rules>` 包含 phase_research 专属规则（6+ 条）
7. `<success_criteria>` 包含 phase_research 专属标准（5+ 条）
</success_criteria>

<output>
After completion, create `.clinpub/phases/05-pre-phase-research/05-02-SUMMARY.md`
</output>
