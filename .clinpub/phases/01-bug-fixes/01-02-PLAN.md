---
phase: 01-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/clinpub/data-prep.md
  - pipeline/workflows/data-prep.md
autonomous: true
requirements: [BUG-02]

must_haves:
  truths:
    - "用户再次执行 `/clinpub-data-prep` 时，命令入口检测 project_config.yml 是否存在并验证关键字段"
    - "检测到已初始化后自动触发全链路刷新（profile -> spec -> config）"
    - "刷新完成后进入正常的用户讨论流程（discuss_cleaning_strategy）"
    - "不增加任何 PreToolUse hook 逻辑"
  artifacts:
    - path: "commands/clinpub/data-prep.md"
      provides: "Command-level re-entry detection at entry point"
      contains: "project_config.yml"
    - path: "pipeline/workflows/data-prep.md"
      provides: "Refresh workflow step (reinit_data_prep)"
      contains: "reinit_data_prep"
  key_links:
    - from: "commands/clinpub/data-prep.md"
      to: "project_config.yml"
      via: "fs.existsSync / Bash test 检查文件存在性 + 验证关键字段"
      pattern: "project_config.yml"
    - from: "pipeline/workflows/data-prep.md"
      to: "scripts/data_profiler.py"
      via: "reinit_data_prep 步骤中重新运行 profiler 更新 profile"
      pattern: "data_profiler.py"
    - from: "pipeline/workflows/data-prep.md"
      to: "pipeline/templates/spec.md"
      via: "reinit_data_prep 步骤中基于更新后的 profile 填充 spec"
      pattern: "spec.md"
---

<objective>
修复 BUG-02：用户修改清洗数据需求时，Phase 1 所有关联文件联动更新

Purpose: 用户完成 Phase 1 后想修改清洗需求时，再次执行 `/clinpub-data-prep` 应自动检测项目已初始化，触发 profile -> spec -> config 全链路刷新，而不是直接进入讨论阶段。
Output:
- `commands/clinpub/data-prep.md` — 命令入口添加重新进入检测逻辑
- `pipeline/workflows/data-prep.md` — 工作流头部添加 `reinit_data_prep` 刷新步骤
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@commands/clinpub/data-prep.md
@pipeline/workflows/data-prep.md
@.clinpub/phases/01-bug-fixes/01-CONTEXT.md

<!-- 关键决策引用 -->
<!-- D-05: 联动更新通过命令入口触发，检测 project_config.yml 是否存在 -->
<!-- D-06: 如果已初始化，自动执行：重新跑 profile -> 重新生成 spec -> 同步 config -->
<!-- D-07: 不增加 PreToolUse hook 逻辑 -->

<!-- 关联参考文件 -->
@pipeline/templates/project_config.yml
@pipeline/templates/spec.md
@scripts/data_profiler.py
</context>

<tasks>

<task type="auto">
<name>Task 1: data-prep 命令入口添加重新进入检测逻辑</name>
<files>commands/clinpub/data-prep.md</files>
<read_first>
- `commands/clinpub/data-prep.md` — 当前薄命令（36 行），在 `<process>` 中只有一条指令
- `pipeline/templates/project_config.yml` — 模板结构，了解关键字段（project.name, variables.outcome, paths.raw_data）
- `.clinpub/phases/01-bug-fixes/01-CONTEXT.md` — D-05（检测触发点）、D-07（不增加 hook 逻辑）
</read_first>
<action>
修改 `commands/clinpub/data-prep.md` 的 `<process>` 部分，在 "Execute the data-prep workflow" 指令前插入重新进入检测指令块：

1. 在 `<process>` 标签后的第一行添加如下指令：

   ```markdown
   ## 重新进入检测（D-05/D-07）

   执行工作流之前，检查项目是否已初始化：

   1. 检查 `project_config.yml` 是否存在于项目根目录
   2. 如果存在，验证关键字段是否有效：
      - `project.name` 非空（不是默认值 "项目名称"）
      - `variables.outcome` 非空
      - `paths.raw_data` 对应的目录存在且有数据文件
   3. **如果所有检查通过** -> 输出 "检测到已有项目配置（project_config.yml），执行自动刷新流程..."，然后执行下面的 step `reinit_data_prep`（D-06 全链路刷新）
   4. **如果任何检查不通过** -> 输出 "未检测到完整项目配置，进入全新数据清洗流程"，然后进入正常的 data-prep 工作流
   5. 注意：不添加任何 PreToolUse hook 逻辑（D-07），所有检测在命令层面完成
   ```

2. 保持现有的 `Execute the data-prep workflow from @./pipeline/workflows/data-prep.md end-to-end.` 行不变（作为未初始化的流程路径）

3. 在 `<objective>` 中追加一行描述重新进入场景：
   ```
   Supports re-entry: if project_config.yml exists with valid fields, auto-refreshes profiles and specs before entering discussion.
   ```
</action>
<acceptance_criteria>
- grep -c "project_config.yml" commands/clinpub/data-prep.md 返回 >= 2（至少出现 2 次：检测条件和验证描述）
- grep -n "project_config.yml" commands/clinpub/data-prep.md 的输出显示第 1 处是检测逻辑、第 2 处是字段验证
- grep -c "重新进入检测\|re-entry\|Re-entry" commands/clinpub/data-prep.md 返回 >= 1
- grep -c "reinit_data_prep" commands/clinpub/data-prep.md 返回 >= 1（刷新步骤引用）
- grep -c "PreToolUse\|hook" commands/clinpub/data-prep.md 原始 0（排除已有的 header 文本后无新增 hook 引用，D-07 合规）
</acceptance_criteria>
<verify>
  <automated>grep -n "project_config.yml" commands/clinpub/data-prep.md</automated>
  <automated>grep -c "reinit_data_prep" commands/clinpub/data-prep.md</automated>
</verify>
<done>data-prep 命令入口在 `<process>` 中有完整的重新进入检测：文件存在性检查 + 关键字段验证 + 双向路由（刷新 vs 全新流程）</done>
</task>

<task type="auto">
<name>Task 2: data-prep 工作流添加 reinit_data_prep 刷新步骤</name>
<files>pipeline/workflows/data-prep.md</files>
<read_first>
- `pipeline/workflows/data-prep.md` — 当前 7 个步骤（discuss_cleaning_strategy -> detect_data_structure -> execute_cleaning -> validate_output -> checkpoint_confirm -> milestone），steps 使用 `<step>` 标签
- `scripts/data_profiler.py` — 第 328-348 行的 main() 函数，接受 `--output` 参数写 JSON 文件；第 201-325 行的 `profile_data()` 函数返回完整 profile
- `pipeline/templates/spec.md` — Mustache 占位符模板，含 `{{study_title}}`、`{{study_type}}`、`{{primary_outcome}}`、`{{N}}`、`{{phase_number}}` 等
- `pipeline/templates/project_config.yml` — YAML 结构，含 variables、paths、analysis 等小节
- `.clinpub/phases/01-bug-fixes/01-CONTEXT.md` — D-06（全链路刷新步骤：profile -> spec -> config）
</read_first>
<action>
在 `pipeline/workflows/data-prep.md` 中插入新的 `<step name="reinit_data_prep" priority="first">` 作为 `<process>` 中的第一个步骤，位于现有的 `discuss_cleaning_strategy` 之前。

完整插入的步骤内容：

```markdown
<step name="reinit_data_prep" priority="first">
当检测到项目已初始化时（由命令入口 data-prep.md 触发），执行全链路文件刷新：

1. **重新运行 profile（更新变量字典）**
   - 定位原始数据文件（从 `project_config.yml` 的 `paths.raw_data` 字段获取路径，查找 `01_RawData/` 下的第一个 CSV/XLSX 文件）
   - 执行：`python scripts/data_profiler.py {raw_data_filepath} --output {preprocessed_path}/data_profile.json`
   - 读取输出的 `data_profile.json`，获取最新的变量列表、缺失模式、变量角色等

2. **重新生成 spec 模板（更新分析规范）**
   - 读取 `data_profile.json` 的变量摘要和 `project_config.yml` 的项目配置
   - 基于 `pipeline/templates/spec.md` 模板，填充以下 Mustache 占位符：
     - `{{study_title}}` -> 从 `project_config.yml` 的 `project.name` 读取
     - `{{study_type}}` -> 从 `project_config.yml` 的 `project.design` 读取
     - `{{N}}` -> 从 `data_profile.json` 的 `n_rows` 读取
     - `{{primary_outcome}}` -> 从 `project_config.yml` 的 `variables.outcome` 读取
     - `{{phase_number}}` -> "1"
     - `{{phase_name}}` -> "Data Preparation"
     - `{{date}}` -> 当前日期
   - 未更改的占位符保持原样（留给 Phase 2 填充）
   - 输出到 `03_AnalysisMethods/01-spec.md`（覆盖旧文件）

3. **同步更新 project_config.yml**
   - 读取当前 `project_config.yml`
   - 根据 `data_profile.json` 的 `role_summary` 更新：
     - `variables.outcome`：如果 profile 检测到 outcome 角色变量且当前 config 中 outcome 为空，填充第一个 outcome 变量名
     - `variables.covariates`：从 profile 的 covariate 角色变量更新列表
     - `variables.group_variable`：从 profile 的 exposure 角色变量更新
   - 仅更新变量相关字段，保留用户自定义的 `project.name`、`analysis` 阈值等手动配置
   - 写回 `project_config.yml`（覆盖但不破坏用户手动编辑的非变量字段）

4. **向用户报告变更摘要**
   - 输出格式：
     ```
     ### 刷新完成
     - Profile 更新: {n_variables} 个变量，{n_rows} 行
     - Spec 更新: 填充了 {n_filled_fields} 个字段
     - Config 更新: {changed_fields} 字段已同步
     - 下一步: 清理策略讨论
     ```
</step>
```

插入位置规则：
- 放在 `<process>` 标签后的第一个 `<step>` 位置
- 确保 `<step name="discuss_cleaning_strategy" priority="first">` 紧接着在 `reinit_data_prep` 之后
- 更新 `<purpose>` 区域的描述，追加一句："Supports re-entry refresh: re-runs profile, spec, and config when project is already initialized."
</action>
<acceptance_criteria>
- grep -c "reinit_data_prep" pipeline/workflows/data-prep.md 返回 1（步骤定义存在）
- reinit_data_prep 步骤在 discuss_cleaning_strategy 步骤之前（在文件中的行号更小）
- grep -c "data_profiler.py" pipeline/workflows/data-prep.md 返回 >= 1（profile 重新运行）
- grep -c "spec.md" pipeline/workflows/data-prep.md 返回 >= 1（spec 重新生成）
- grep -c "project_config.yml" pipeline/workflows/data-prep.md 返回 >= 1（config 更新）
- grep -c "discuss_cleaning_strategy" pipeline/workflows/data-prep.md 仍然 >= 1（原有的讨论步骤保留）
</acceptance_criteria>
<verify>
  <automated>grep -n "reinit_data_prep" pipeline/workflows/data-prep.md</automated>
  <automated>grep -n "data_profiler.py" pipeline/workflows/data-prep.md</automated>
  <automated>grep -c "discuss_cleaning_strategy" pipeline/workflows/data-prep.md</automated>
</verify>
<done>工作流头部新增 `reinit_data_prep` 步骤，包含 profile 重新运行 -> spec 模板重新生成 -> config 同步更新 -> 用户通知 四个子步骤</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> data-prep command | 用户执行命令触发文件刷新，刷新过程可能覆盖用户手动编辑的配置文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Refresh 流程覆盖用户手动编辑的 project_config.yml | mitigate | 采取字段级别的 merge 策略：仅更新 profile 相关的变量字典字段（variables.outcome、variables.covariates、variables.group_variable），保留 project.name、analysis.thresholds 等用户手动配置的非变量字段 |
| T-01-05 | Spoofing | 未初始化的 project_config.yml 触发性刷新（从 data2idea 流程的 idea/to_project_config.yml 复制而来，未经过完整 Phase 0 初始化） | mitigate | 命令入口检测时验证关键字段：project.name 非空且不是默认值 "项目名称"，variables.outcome 非空，paths.raw_data 对应目录存在且有数据文件。仅当所有检查通过才触发刷新 |
| T-01-06 | Elevation of Privilege | 通过覆盖 spec.md 注入恶意内容 | accept | spec.md 是分析规范文档，最终由 AI agent 读取并执行分析。恶意注入风险极低，且 agent-contracts 有独立的安全边界 |
</threat_model>

<verification>
1. `commands/clinpub/data-prep.md` 的 `<process>` 包含重新进入检测逻辑
2. `pipeline/workflows/data-prep.md` 的 `<step name="reinit_data_prep">` 在 discuss_cleaning_strategy 之前
3. reinit_data_prep 步骤包含 profile 重新运行指令
4. reinit_data_prep 步骤包含 spec 模板重新生成指令
5. reinit_data_prep 步骤包含 config 同步更新指令
6. reinit_data_prep 步骤包含用户通知指令
7. 未在 data-prep.md 或 workflow 中新增 PreToolUse hook 逻辑
</verification>

<success_criteria>
- [ ] `grep -c "reinit_data_prep" pipeline/workflows/data-prep.md` >= 1
- [ ] `grep -c "data_profiler.py" pipeline/workflows/data-prep.md` >= 1
- [ ] `grep -c "project_config.yml" commands/clinpub/data-prep.md` >= 2
- [ ] 重新进入检测逻辑包含 project_config.yml 存在性 + 关键字段验证
</success_criteria>

<output>
After completion, create `.clinpub/phases/01-bug-fixes/01-02-SUMMARY.md`
</output>
