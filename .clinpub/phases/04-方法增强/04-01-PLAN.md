---
phase: 04-方法增强
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/references/comparison-methods.md
autonomous: true
requirements: [METH-02]

must_haves:
  truths:
    - "Analyst 查阅 comparison-methods.md 后能按组数和结局类型自动选择正确的检验方法（D-08 ~ D-13）"
    - "两组连续结局按正态性+方差齐性自动分支：t检验 / Welch t / Mann-Whitney U"
    - "两组分类结局按理论频数自动分支：卡方 / Fisher 精确检验"
    - "三组+连续结局按正态性+方差齐性自动分支：ANOVA+Bonferroni/Tukey / Welch ANOVA+Games-Howell / Kruskal-Wallis+Dunn"
    - "三组+分类结局自动选择卡方 / Fisher 精确检验 + 列联表事后分析"
    - "配对/重复测量有独立分支：paired t / Wilcoxon 符号秩 / 重复测量 ANOVA / Friedman"
    - "每种方法附带效应量计算公式：Cohen's d / r=Z/√N / η² / Cramer's V"
    - "decision tree 优先用表格/流程图呈现，比纯文字更直观"
  artifacts:
    - path: "pipeline/references/comparison-methods.md"
      provides: "组间对比方法固化决策树参考文档"
      contains: "comparison-methods"
    - path: "pipeline/references/analysis_methods.md"
      provides: "更新后的方法选择树，交叉引用 comparison-methods.md"
      contains: "comparison-methods.md"
  key_links:
    - from: "pipeline/references/comparison-methods.md"
      to: "pipeline/references/analysis_methods.md"
      via: "决方法择树中交叉引用 comparison-methods.md 作为组间对比细分规则"
      pattern: "comparison-methods"
    - from: "pipeline/references/comparison-methods.md"
      to: "agents/clinpub-planner.md"
      via: "Planner 在构建分析方案时查阅 comparison-methods.md 选择对比方法"
      pattern: "comparison-methods.md"
    - from: "pipeline/references/comparison-methods.md"
      to: "agents/analyst-agent.md"
      via: "Analyst 在生成 R 代码时按 decision tree 选择具体检验函数"
      pattern: "comparison-methods.md"
---

<objective>
创建 `pipeline/references/comparison-methods.md` — 组间对比方法固化决策树参考文档，作为 clinpub 的标准方法选择依据。

Purpose: 实现 METH-02（组间对比方法固化）。当前 `analysis_methods.md` §3.2 中的组间比较部分只有概要描述（"正态→t检验，偏态→Wilcoxon"），缺少完整的决策树。分析师每次需要自行判断，不同项目间结果不一致。本 plan 创建独立参考文档，按组数（2组 vs 3+组）×结局类型（连续/分类）全覆盖，含正态性检验驱动、配对方言、效应量报告规则。

Output:
- `pipeline/references/comparison-methods.md` — 完整决策树参考文档
- `pipeline/references/analysis_methods.md` — 在 §3.2 中追加交叉引用
</objective>

<execution_context>
@./pipeline/workflows/execute-plan.md
@./pipeline/templates/summary.md
</execution_context>

<context>
@.clinpub/phases/04-方法增强/04-CONTEXT.md

# 格式参考
@pipeline/references/analysis_methods.md
  — 使用其表格结构、二级标题分隔风格、代码块示例格式

# 已有的搜索工具
@scripts/tavily_search.py
@scripts/ncbi_search.py

<interfaces>
<!-- 现有方法选择表的行模式 -->
From pipeline/references/analysis_methods.md §二 Step 2:
| 数据特征 | 推荐方法方向 | 原因 |
|---------|-------------|------|
| 2 组 + 连续结局 | t检验 / Wilcoxon + 混合模型 | 基础比较 |

From pipeline/references/analysis_methods.md §3.2:
#### TwoGroupComparison（两组比较）
- **方法选择**：
  - 正态分布 + 方差齐 → 独立样本 t 检验
  - 偏态分布 → Wilcoxon 秩和检验
  - 配对设计 → 配对 t 检验 / Wilcoxon 符号秩检验

#### MultiGroupComparison（多组比较）
- **方法选择**：
  - 正态 + 方差齐 → ANOVA + Tukey HSD
  - 偏态 → Kruskal-Wallis + Dunn 事后比较
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建组间对比方法决策树文档</name>
  <files>
    pipeline/references/comparison-methods.md
  </files>
  <read_first>
    .clinpub/phases/04-方法增强/04-CONTEXT.md (D-08 ~ D-13 决策详情)
    pipeline/references/analysis_methods.md (现有格式模板)
  </read_first>
  <action>
    创建 `pipeline/references/comparison-methods.md`，作为独立的组间对比方法决策树参考文档。

    文档结构如下：

    # 组间对比方法决策树

    > 本文件定义组间对比的标准化方法选择流程，供 clinpub-planner 和 analyst-agent 在分析规划/执行时查阅。
    > 配合 `analysis_methods.md` §3.2 使用，提供更细粒度的分支规则。

    ## 一、方法选择概览树

    用 ASCII 流程图或 Markdown 表格呈现整体决策流程。例如表格：

    | 组数 | 结局类型 | 正态性 | 方差齐性 | 推荐方法 | 事后比较 | 效应量 |
    |------|---------|--------|---------|---------|---------|-------|
    | 2组 | 连续 | 正态 | 齐 | 独立样本 t 检验 | — | Cohen's d |
    | 2组 | 连续 | 正态 | 不齐 | Welch t 检验 | — | Cohen's d |
    | 2组 | 连续 | 非正态 | — | Mann-Whitney U 检验 | — | r = Z/√N |
    | 2组 | 分类 | — | 理论频数≥5 | 卡方检验 | — | Cramer's V / φ |
    | 2组 | 分类 | — | 理论频数<5 | Fisher 精确检验 | — | Cramer's V / φ |
    | 3+组 | 连续 | 正态 | 齐 | 单因素 ANOVA | Bonferroni / Tukey | η² / partial η² |
    | 3+组 | 连续 | 正态 | 不齐 | Welch ANOVA | Games-Howell | η² / partial η² |
    | 3+组 | 连续 | 非正态 | — | Kruskal-Wallis 检验 | Dunn 检验 | η² (H 统计量) |
    | 3+组 | 分类 | — | 理论频数≥5 | 卡方检验 | 列联表事后分析 | Cramer's V |
    | 3+组 | 分类 | — | 理论频数<5 | Fisher 精确检验 | 列联表事后分析 | Cramer's V |

    ## 二、两组比较细则（D-08, D-09）

    ### 2.1 两组—连续结局（D-08）

    自动检测流程：
    1. **正态性检验**：Shapiro-Wilk 检验（每组 N<50）或 Kolmogorov-Smirnov（N≥50）
       - 两组均正态 → 走参数路径
       - 任一组非正态 → 走非参数路径
    2. **方差齐性检验**（仅参数路径）：Levene 检验或 F 检验
       - 方差齐 → 独立样本 t 检验
       - 方差不齐 → Welch t 检验
    3. **非参数路径** → Mann-Whitney U 检验

    **R 代码参考：**
    ```r
    # 正态性检验
    shapiro.test(data$value[data$group == "A"])
    shapiro.test(data$value[data$group == "B"])
    
    # 方差齐性检验（参数路径）
    car::leveneTest(value ~ group, data = data)
    
    # 执行
    if (both_normal && var_equal) {
      t.test(value ~ group, data = data, var.equal = TRUE)
    } else if (both_normal && !var_equal) {
      t.test(value ~ group, data = data, var.equal = FALSE)  # Welch
    } else {
      wilcox.test(value ~ group, data = data)  # Mann-Whitney U
    }
    ```

    ### 2.2 两组—分类结局（D-09）

    1. 构建列联表：`table(group, outcome)`
    2. 计算理论频数：`chisq.test(tbl)$expected`
    3. 所有格理论频数 ≥5 → Pearson 卡方检验
    4. 任一理论频数 <5 → Fisher 精确检验

    ```r
    tbl <- table(data$group, data$outcome)
    chisq_test <- chisq.test(tbl)
    if (any(chisq_test$expected < 5)) {
      fisher.test(tbl)  # Fisher 精确检验
    } else {
      chisq_test       # 卡方检验
    }
    ```

    ## 三、三组以上比较细则（D-10, D-11）

    ### 3.1 三组+—连续结局（D-10）

    自动检测流程：
    1. **正态性检验**：每组的 Shapiro-Wilk 或 K-S
    2. **方差齐性检验**（仅参数路径）：Levene 检验或 Bartlett 检验
    3. **分支路径**：
       - 全部正态 + 方差齐 → **单因素 ANOVA** + Tukey HSD 事后比较（平衡设计）或 Bonferroni 校正（非平衡）
       - 全部正态 + 方差不齐 → **Welch ANOVA** + Games-Howell 事后比较
       - 任一组非正态 → **Kruskal-Wallis 检验** + Dunn 事后比较（Bonferroni 校正）
    
    ```r
    # 正态性：对每组做
    by(data$value, data$group, shapiro.test)
    
    # 方差齐性
    car::leveneTest(value ~ group, data = data)
    
    # 执行
    if (all_normal && var_equal) {
      fit <- aov(value ~ group, data = data)
      TukeyHSD(fit)
    } else if (all_normal && !var_equal) {
      oneway.test(value ~ group, data = data)  # Welch ANOVA
      # Games-Howell: 需 PMCMRplus 包或手动计算
    } else {
      kruskal.test(value ~ group, data = data)
      # Dunn 事后: FSA::dunnTest(value ~ group, data = data, method = "bonferroni")
    }
    ```

    ### 3.2 三组+—分类结局（D-11）

    逻辑同两组分类（§2.2），加列联表事后分析：
    - 卡方检验显著 → 做标准化残差分析或两两列联表比较（Bonferroni 校正）

    ```r
    tbl <- table(data$group, data$outcome)
    chisq_test <- chisq.test(tbl)
    if (any(chisq_test$expected < 5)) {
      fisher.test(tbl, simulate.p.value = TRUE)
    } else {
      chisq_test
      # 事后分析：标准化残差
      round(chisq_test$residuals, 2)
    }
    ```

    ## 四、配对/重复测量设计（D-12）

    | 设计 | 变量类型 | 正态性 | 方法 |
    |------|---------|--------|------|
    | 两组配对 | 连续 | 正态 | 配对 t 检验 |
    | 两组配对 | 连续 | 非正态 | Wilcoxon 符号秩检验 |
    | 两组配对 | 分类 | — | McNemar 检验 |
    | 多组重复测量 | 连续 | 正态+球性 | 重复测量 ANOVA |
    | 多组重复测量 | 连续 | 非正态/球性不齐 | Friedman 检验 |
    | 多组重复测量 | 分类 | — | 广义估计方程 (GEE) / Cochran's Q |

    ```r
    # 配对 t 检验
    t.test(pre, post, paired = TRUE)
    
    # Wilcoxon 符号秩检验
    wilcox.test(pre, post, paired = TRUE)
    
    # 重复测量 ANOVA
    library(lme4)
    m <- lmer(value ~ time * group + (1 | subject), data = data)
    anova(m)
    
    # Friedman 检验
    friedman.test(value ~ time | subject, data = data)
    ```

    ## 五、效应量报告标准（D-13）

    | 检验方法 | 效应量 | 公式 | 解释基准 |
    |---------|--------|------|---------|
    | 独立样本 t 检验 | Cohen's d | d = (M₁−M₂) / SD_pooled | 0.2小/0.5中/0.8大 |
    | Mann-Whitney U | r | r = Z/√N | 0.1小/0.3中/0.5大 |
    | 单因素 ANOVA | η² / partial η² | η² = SS_效应 / SS_总 | 0.01小/0.06中/0.14大 |
    | Welch ANOVA | η² | 同上 | 同上 |
    | Kruskal-Wallis | η² (H) | η² = (H−k+1)/(N−k) | 同上 |
    | 卡方检验 | Cramer's V / φ | V = √(χ²/(N·min(r−1,c−1))) | 0.1小/0.3中/0.5大 |
    | 配对 t 检验 | Cohen's d_z | d_z = t/√n | 同上 Cohen's d |
    | Wilcoxon 符号秩 | r | r = Z/√N | 0.1小/0.3中/0.5大 |

    ```r
    # Cohen's d
    library(effsize)
    cohens_d(data$value, data$group)
    
    # η²
    library(lsr)
    etaSquared(aov_model)
    
    # Cramer's V
    library(rcompanion)
    cramerV(tbl)
    ```

    ## 六、执行步骤（供 clinpub-executor 使用）

    1. **读数据**：从 cleaned.csv 读取结局变量和分组变量
    2. **检测组数**：unique(group) 的长度 → 2组 或 3+组
    3. **检测结局类型**：unique(outcome) 的数量 + 数据类型 → 连续/分类
    4. **按决策树选择方法**（查上面各节）
    5. **检验前提条件**（正态性、方差齐性、理论频数）
    6. **执行主检验**
    7. **若显著且组数>2 → 事后比较**
    8. **计算效应量 + 95% CI**
    9. **输出**：统计结果表 + 效应量 + 可视化（箱线图/柱状图 + 显著性标注）

    ## 引用文献

    附件关键参考文献（如有），如：
    - Cohen, J. (1988). *Statistical Power Analysis for the Behavioral Sciences*.
    - Field, A. (2013). *Discovering Statistics Using IBM SPSS Statistics*.
  </action>
  <verify>
    <automated>bash -c "Test-Path 'pipeline/references/comparison-methods.md' -and (Get-Item 'pipeline/references/comparison-methods.md').Length -gt 5000"</automated>
  </verify>
  <acceptance_criteria>
    1. `pipeline/references/comparison-methods.md` 存在且 >5KB
    2. 文件包含所有 6 个章节标题（"一、方法选择概览树" 到 "六、执行步骤"）
    3. 文件包含 D-08 ~ D-13 所有 13 个决策分支
    4. 文件包含效应量表格（Cohen's d, r, η², Cramer's V）
    5. 文件包含 R 代码参考块（t.test, wilcox.test, aov, kruskal.test 等）
  </acceptance_criteria>
  <done>comparison-methods.md 存在，包含完整的 2组/3+组 × 连续/分类 决策树 + 配对/重复测量分支 + 效应量标准，R 代码示例可执行</done>
</task>

<task type="auto">
  <name>Task 2: 在 analysis_methods.md 中交叉引用 comparison-methods.md</name>
  <files>
    pipeline/references/analysis_methods.md
  </files>
  <read_first>
    pipeline/references/analysis_methods.md (读取 §3.2 组间比较部分)
    pipeline/references/comparison-methods.md (刚创建的文档)
  </read_first>
  <action>
    在 `pipeline/references/analysis_methods.md` 的 §3.2（组间比较）每节下方追加交叉引用：

    **TwoGroupComparison 节（约 line 124）**：
    在 `- **方法选择**：` 块后追加：
    ```markdown
    **完整决策树**：参见 `comparison-methods.md §二` — 含正态性检验驱动的自动分支、方差不齐处理、效应量公式。
    ```

    **MultiGroupComparison 节（约 line 133）**：
    在 `- **方法选择**：` 块后追加：
    ```markdown
    **完整决策树**：参见 `comparison-methods.md §三` — 含 Welch ANOVA、Games-Howell、Kruskal-Wallis+Dunn 分支。
    ```

    **RepeatedMeasures 节（约 line 139）**：
    在 `- **方法选择**：` 块后追加：
    ```markdown
    **完整决策树**：参见 `comparison-methods.md §四` — 含配对 t 检验、Wilcoxon 符号秩、重复测量 ANOVA、Friedman 检验分支。
    ```

    不要修改 comparison-methods.md 的内容。这是纯引用更新。
  </action>
  <verify>
    <automated>bash -c "Select-String -Path 'pipeline/references/analysis_methods.md' -Pattern 'comparison-methods.md' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    1. `analysis_methods.md` 中 ThreeGroupComparison、MultiGroupComparison、RepeatedMeasures 三节下方都出现了 `comparison-methods.md` 的交叉引用
    2. 交叉引用指向正确的章节号（§二、§三、§四）
  </acceptance_criteria>
  <done>analysis_methods.md 中 3 个组间比较小节均已追加 comparison-methods.md 交叉引用</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference doc → agent | 参考文档内容由开发者写入，非用户输入，无注入风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | comparison-methods.md | accept | 静态参考文档，非执行代码，无注入面 |
</threat_model>

<verification>
1. `grep -c "comparison-methods" pipeline/references/analysis_methods.md` >= 3
2. `grep "t.test\|wilcox.test\|aov\|kruskal.test\|chisq.test\|fisher.test" pipeline/references/comparison-methods.md | head -6` 每种检验方法至少出现一次
3. comparison-methods.md 包含 "Cohen's d", "Cramer's V", "η²" 三种效应量
</verification>

<success_criteria>
- comparison-methods.md 完整覆盖所有 13 个决策分支（D-08 ~ D-13）
- 三种效应量有计算公式和 R 代码参考
- analysis_methods.md 中 3 处组间比较小节指向 comparison-methods.md
- 文档可直接作为 clinpub-planner 和 analyst-agent 的方法选择依据
</success_criteria>

<output>
After completion, create `.clinpub/phases/04-方法增强/04-01-SUMMARY.md`
</output>
