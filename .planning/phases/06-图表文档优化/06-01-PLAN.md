---
phase: 6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pipeline/references/r_patterns.md
  - pipeline/references/journal_standards.md
autonomous: true
requirements:
  - CHART-01
---

## Objective

升级 `r_patterns.md` 中的 R 可视化规范，使所有分析输出达到 SCI 期刊投稿级质量。包括优化 `theme_pub()` 主题、补充缺失的图表类型模板、更新字体和尺寸规范。

## Tasks

### Task 1: 优化 theme_pub() 主题

<read_first>
- `pipeline/references/r_patterns.md` — 当前 theme_pub() 定义（§1.2）
- `pipeline/references/journal_standards.md` — 图表技术规范
- `.planning/phases/06-图表文档优化/06-RESEARCH.md` — 改进建议（§3）
</read_first>

<action>
更新 r_patterns.md §1.2 中的 theme_pub() 定义：

1. 将 `base_size` 默认值从 14 改为 10
2. 添加 `base_family = "sans"` 参数（Windows 上映射到 Arial）
3. 将 `legend.position` 默认值从 `"none"` 改为 `"right"`
4. 添加 `legend.title` 和 `legend.text` 样式
5. 添加 `axis.line = element_line(color = "black", linewidth = 0.5)`
6. 将 `axis.ticks` 的 linewidth 显式设为 0.5
7. 将 `strip.text` 添加 `size = rel(0.9)`
8. 更新应用说明文字，说明如何覆盖 legend.position
</action>

<acceptance_criteria>
- `r_patterns.md` 中 `theme_pub` 函数定义包含 `base_size = 10` 和 `base_family = "sans"` 参数
- `legend.position` 默认值为 `"right"`
- 函数定义包含 `axis.line` 设置
- 文件中有关于如何覆盖 `legend.position = "none"` 的说明
</acceptance_criteria>

### Task 2: 新增 KM 生存曲线美化模板

<read_first>
- `pipeline/references/r_patterns.md` — 现有图表类型模板（§2.1-2.8）
- `pipeline/references/analysis_methods.md` — 生存分析方法（§3.4）
- `.planning/phases/06-图表文档优化/06-RESEARCH.md` — KM 曲线建议（§4.1）
</read_first>

<action>
在 r_patterns.md 第二部分末尾（§2.8 之后）新增 §2.9 KM 生存曲线美化模板：

内容包含：
- 意图：展示生存函数随时间的变化，比较两组生存率差异
- 模式：使用 `survival::survfit()` + `survminer::ggsurvplot()`，应用 `theme_pub()` 主题
- 配色使用 `c("#0072B5", "#BC3C29")`（Nature 双色）
- 包含风险表（risk.table = TRUE）
- 包含 p 值和方法标注（pval = TRUE, pval.method = TRUE）
- 适用条件：时间-事件数据、两组或多组生存比较
- 不适用条件：非生存数据、仅关注终点而非时间过程
</acceptance_criteria>

- r_patterns.md 包含 §2.9 KM 生存曲线美化章节
- 代码示例使用 `survminer::ggsurvplot()` + `theme_pub()`
- 包含适用条件和不适用条件

### Task 3: 新增相关性矩阵热图模板

<read_first>
- `pipeline/references/r_patterns.md` — 现有图表类型模板
- `pipeline/references/analysis_methods.md` — 相关性分析方法（§3.6）
</read_first>

<action>
在 r_patterns.md §2.9 之后新增 §2.10 相关性矩阵热图模板：

内容包含：
- 意图：展示多个连续变量间的相关性结构
- 模式：使用 `cor()` + `ggcorrplot::ggcorrplot()`，应用 `theme_pub()` 主题
- 支持 Spearman（偏态）和 Pearson（正态）方法
- 配色使用蓝白红三色方案 `c("#0072B5", "white", "#BC3C29")`
- 显示下三角（type = "lower"），标注相关系数（lab = TRUE）
- 适用条件：多变量关联探索、回归前变量筛选
- 不适用条件：仅两个变量（用散点图更直观）、分类变量（用卡方检验）
</acceptance_criteria>

- r_patterns.md 包含 §2.10 相关性矩阵热图章节
- 代码示例使用 `ggcorrplot` + `theme_pub()`
- 包含适用条件和不适用条件

### Task 4: 更新字体和尺寸规范

<read_first>
- `pipeline/references/r_patterns.md` — §1.3 图表保存规范
- `pipeline/references/journal_standards.md` — 图表技术规范
</read_first>

<action>
在 r_patterns.md §1.3 中添加字体规范说明：

1. 在"所有输出必须 ≥300 DPI，Arial ≥8pt"后添加：
   - 说明 R 中 `theme_pub(base_family = "sans")` 在 Windows 上默认使用 Arial
   - 说明 Linux/macOS 上可能需要 `extrafont` 包来加载 Arial
   - 提供 `extrafont::font_import()` 和 `extrafont::loadfonts()` 的使用说明
2. 更新尺寸标准表格，添加 Nature 系列的单栏 89mm/双栏 183mm 作为参考
</acceptance_criteria>

- r_patterns.md 包含字体族的跨平台说明
- 提供了 Linux/macOS 上使用 extrafont 的指导
- 尺寸标准表格包含 Nature 系列参考值
