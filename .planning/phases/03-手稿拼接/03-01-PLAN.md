---
phase: 03-手稿拼接
plan: 01
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - pipeline/workflows/writing.md
autonomous: false
requirements: [WRITE-01]

must_haves:
  truths:
    - "撰写流程按 Intro → Methods → Results → Discussion 顺序逐段进行（D-01）"
    - "每段撰写前 reference-agent 自动搜索文献（D-06）"
    - "每段撰写后 pause 供用户审阅确认，用户确认后才继续下一段"
    - "同一个 writer-agent 分 4 轮对话写 4 段（D-02），每轮仅给该段所需上下文"
    - "上下文自动读取项目数据（spec → Methods, analysis outputs → Results, 文献 → Intro/Discussion）"
    - "Methods 段从 spec + analysis pipeline outputs 自动生成初稿（D-05）"
    - "各段写时查询共享引用库（reference_library.json），已存在则复用编号（D-09）"
    - "各段使用占位符（{{Table:N}} {{Figure:N}} {{Method:name}}）进行交叉引用"
    - "全文 >5000 字，自然成段论述（D-04）"
  artifacts:
    - path: "pipeline/workflows/writing.md"
      provides: "Revised writing workflow with per-section sequential pipeline, reference-agent pre-search per section, user review pause between sections"
      contains: "draft_section_introduction"
    - path: "pipeline/references/reference-library.md"
      provides: "Shared reference library spec (created by Plan 03-02, consumed by this plan)"
      contains: "reference_library.json"
  key_links:
    - from: "pipeline/workflows/writing.md"
      to: "pipeline/references/reference-library.md"
      via: "各段撰写流程查询共享引用库"
      pattern: "reference-library.md"
    - from: "pipeline/workflows/writing.md"
      to: "agents/reference-agent.md"
      via: "每段前调用 reference-agent 搜索文献"
      pattern: "reference-agent"
    - from: "pipeline/workflows/writing.md"
      to: "agents/writer-agent.md"
      via: "分轮调用 writer-agent 撰写各段"
      pattern: "writer-agent"
    - from: "pipeline/workflows/writing.md"
      to: "05_Manuscript/sections/"
      via: "各段写入 sections/ 目录"
      pattern: "05_Manuscript/sections"
---

<objective>
将 `pipeline/workflows/writing.md` 从单次 IMRAD 全篇撰写改造为逐段顺序撰写流程，支持每段前 reference-agent 文献搜索、writer-agent 分 4 轮对话写 4 段、用户审阅 pause 衔接、共享引用库管理。

Purpose: 实现 WRITE-01（各段独立撰写，各自完成引用）。当前 workflow（`draft_imrad_chapters`）一次写入全部章节，不符合 D-01（逐段 pause 审阅）。需要改为按 Intro → Methods → Results → Discussion 顺序，每段写前 reference-agent 搜索文献，写后用户审阅，审毕再继续下一段。

Output:
- `pipeline/workflows/writing.md` — 修改后的分段撰写工作流
</objective>

<execution_context>
@./pipeline/workflows/writing.md
@./pipeline/references/reference-library.md
@./agents/writer-agent.md
@./agents/reference-agent.md
@./.planning/phases/03-手稿拼接/03-CONTEXT.md
</execution_context>

<context>
@./pipeline/workflows/writing.md
@./pipeline/references/reference-library.md
@./agents/writer-agent.md
@./agents/reference-agent.md

<!-- 关键决策 (from 03-CONTEXT.md) -->
<!-- D-01: IMRAD 顺序流水线 (Intro → Methods → Results → Discussion), pause 审阅 -->
<!-- D-02: 同一 writer-agent, 4 轮对话, 不扩展 agent 定义 -->
<!-- D-03: 自动读取项目数据 -->
<!-- D-04: 全文 >5000 字，自然成段论述 -->
<!-- D-05: Methods 从 spec + analysis outputs 自动生成初稿 -->
<!-- D-06: 每段前 reference-agent 搜索文献 -->
<!-- D-09: 全局统一编号，共享引用库 -->
<!-- D-11: 占位符 ({{Table1}} {{MethodX}})，拼接时替换 -->
<!-- D-12: Table/Figure 全局编号，IMRAD 顺序 -->
<!-- D-13: Results 风格：描述关键发现 + 指向图表 -->

<interfaces>
<!-- 各段上下文来源（D-03 自动读取） -->
Methods 上下文:
- 来源: project_config.yml → study_type, variables
- 来源: pipeline/templates/study_types/{study_type}.md
- 来源: 03_AnalysisMethods/*/README.md (分析方法说明)
- 来源: scripts/data_profiler.py 执行的 profile 输出

Results 上下文:
- 来源: 04_Outputs/*/ 所有分析输出的 figure + table + README
- README 必须包含「Results」subsection (agent-contracts.md 定义)

Introduction 上下文:
- 来源: Reference/reference_library.json (已有引用库)
- 来源: Reference/citation_map.md (引用映射)
- 来源: Reference/literature_notes/ (文献摘要)

Discussion 上下文:
- 来源: Reference/reference_library.json
- 来源: Reference/citation_map.md
- 来源: project_config.yml (target_journal, scope)

<!-- Shared reference library 使用 -->
引用库路径: Reference/reference_library.json
查询规则:
  1. 搜索 citation_key 是否存在
  2. 存在 → 复用 id, 在 sections_used 追加当前段名
  3. 不存在 → 分配 max_id+1, 写入库

<!-- 占位符使用 -->
- Table: {{Table:N}} 例: {{Table:1}}
- Figure: {{Figure:N}} 例: {{Figure:1}}
- Method: {{Method:name}} 例: {{Method:BaselineTable}}
- Section: {{Section:name}} 例: {{Section:methods}}
</interfaces>
</context>

<tasks>

<task type="auto">
<name>Task 1: 重构 workflow 结构 — 替换单次 IMRAD 撰写为逐段顺序流水线</name>
<files>pipeline/workflows/writing.md</files>
<read_first>
- `pipeline/workflows/writing.md` — 当前完整 workflow（143 行），重点是 `draft_imrad_chapters` 步骤（第 46-73 行）
- `pipeline/references/reference-library.md` — Plan 03-02 刚创建的引用管理规范
- `agents/writer-agent.md` — 第 53-101 行各段撰写逻辑（draft_methods, draft_results, draft_introduction, draft_discussion）
- `agents/reference-agent.md` — 第 35-55 行文献搜索策略（Phase 3 全搜索 + 章节补充搜索）
- `.planning/phases/03-手稿拼接/03-CONTEXT.md` — D-01~D-06, D-09, D-11~D-13
- `pipeline/contexts/writing.md` — 当前撰写上下文（第 11-17 行章节写作顺序）
</read_first>
<action>
修改 `pipeline/workflows/writing.md`，做以下全部变更：

**a) 更新 `<purpose>` 描述**，反映分段撰写模式：

将原有：
```markdown
description: "Phase 3 orchestration: Reference Agent performs literature search, Writer Agent drafts IMRAD manuscript following study type template. Chapter-by-chapter writing with supplementary search between chapters."
```
替换为：
```markdown
description: "Phase 3 orchestration: IMRAD sequential pipeline (Introduction → Methods → Results → Discussion). Each section: reference-agent pre-search → writer-agent draft → user review pause. Shared reference library (JSON) for cross-section deduplication. Placeholders for cross-references. Final concatenation in Plan 03-03."
```

**b) 更新 `<required_reading>`，追加 reference-library.md 引用**：

在 `@./pipeline/references/checkpoints.md` 行后追加：
```
@./pipeline/references/reference-library.md
```

**c) 替换 `draft_imrad_chapters` 步骤为逐段撰写循环**：

删除当前第 46-73 行的 `draft_imrad_chapters` 步骤全部内容（包括 Wave 1~Wave 5）。

替换为以下内容：

```markdown
<step name="sequential_section_writing" priority="high">
以 IMRAD 顺序（Introduction → Methods → Results → Discussion）逐段撰写。每段分三阶段：reference-agent 文献预搜索 → writer-agent 撰写 → 用户审阅。

**顺序**: Introduction → Methods → Results → Discussion（D-01）

**核心约束**:
- 同一 writer-agent 分 4 轮对话（D-02），每轮仅给该段上下文
- 不扩展 writer-agent.md 的角色定义（D-02）
- 全文 >5000 字，自然成段论述，不用 bullet point（D-04）
- Results 段落风格：描述关键发现 + 指向图表，不使用 "As shown in Table X" 作为段落开头（D-13）

---

### 逐段循环

对每段（section in [introduction, methods, results, discussion]）执行以下三步：

#### Step A: Reference-Agent 文献预搜索（D-06）

调用 reference-agent 搜索该段的 PubMed 文献。搜索策略（参考 `agents/reference-agent.md`）:

| 段落 | 搜索关键词 | 引用量 |
|------|-----------|--------|
| Introduction | disease + exposure + outcome + population | 10-15 篇 |
| Methods | methodology, standard guidelines, published protocols | 3-5 篇 |
| Results | （非必要，仅对比时搜索） | 0-3 篇 |
| Discussion | 对比同类研究 + 机制解释 + 临床意义相关文献 | 15-25 篇 |

搜索后更新 `Reference/reference_library.json`：
1. 读取已有库
2. 新引用逐条去重（检查 `citation_key`）
3. 分配新 ID（max_id + 1）
4. 写入库
5. 更新 `Reference/MANIFEST.yaml`

#### Step B: Writer-Agent 撰写（D-02, D-03）

调用 writer-agent（同一 agent，不扩展 role 定义），仅传入该段所需的上下文：

| 段落 | 上下文来源（D-03 自动读取） |
|------|---------------------------|
| Introduction | Reference/reference_library.json（已有引用）、Reference/citation_map.md、Reference/literature_notes/ |
| Methods | project_config.yml（study_type, variables）、pipeline/templates/study_types/{type}.md、03_AnalysisMethods/*/README.md |
| Results | 04_Outputs/*/（所有 figure + table + README，其中 README 必须有「Results」subsection）|
| Discussion | Reference/reference_library.json、Reference/citation_map.md、project_config.yml（target_journal, scope）|

撰写规则：
- Methods 从 spec + analysis pipeline outputs 自动生成初稿（D-05）
- 使用 shared reference library (`Reference/reference_library.json`) 查询已有引用
- 使用占位符进行交叉引用：`{{Table:N}}` `{{Figure:N}}` `{{Method:name}}` `{{Section:name}}`（D-11）
- 自然成段论述，不使用 bullet point（D-04）
- 每段写入 `05_Manuscript/sections/`，命名规则：
  - `05_Manuscript/sections/01-introduction.md`
  - `05_Manuscript/sections/02-methods.md`
  - `05_Manuscript/sections/03-results.md`
  - `05_Manuscript/sections/04-discussion.md`

#### Step C: 用户审阅暂停（checkpoint）（D-01）

段撰写完成后，pause 等待用户审阅：

```markdown
## {段名} 初稿完成 — 请审阅

已写入 `05_Manuscript/sections/{文件名}`。

### 审阅要点
- [ ] {段名}的结构和内容是否符合预期
- [ ] 引用是否准确，引用量是否合适（{引用量建议}）
- [ ] 占位符是否需要调整或补充
- [ ] 语言风格是否自然（Humanizer 检查通过）
- [ ] 需要修改或补充的内容

### 下一步
- 输入 `通过` / `继续` → 进入下一段撰写
- 提出修改意见 → 调整当前段后重新审阅
```

当用户确认后（`通过` 或 `继续`），进入下一段循环。
</step>
```

**d) 更新 `humanizer_review` 步骤**，改为每段内的自检（非全篇检查）：

将原有的 `humanizer_review` 步骤描述更新，说明 humanizer 检查在每个段落内完成（writer-agent 在写每段时执行 humanizer 自检），不需要单独的全篇 humanizer 步骤。

将第 75-86 行替换为：

```markdown
<step name="humanizer_review" priority="medium">
Humanizer 自检在每个段落撰写时由 writer-agent 内嵌执行（见 writer-agent.md humanizer_rules 节），不再单独执行全篇 humanizer。

在 sequential_section_writing 步骤的 Step B（writer-agent 撰写）中，writer-agent 在每段写入前执行 Humanizer checklist：

| Check | AI Pattern | Fix |
|-------|-----------|-----|
| Paragraph openings | Sequential markers | Content-driven progression |
| Transition words | Repeated formulaic | Specific logical connectors |
| Sentence structure | Uniform patterns | Varied sentence types |
| Conclusions | Hollow or generic | Specific future direction |
| Citations | Impersonal | Author-contextualized |
| Explanations | Over-explaining methods | Just state, don't justify |

如果用户审阅时指出语言问题，writer-agent 直接在该段内修正，不重写全篇。
</step>
```

**e) 更新 `verify_manuscript` 步骤**，追加对分段结构的验证：

在原有验证清单末尾追加：

```
10. 分段的完整性：05_Manuscript/sections/ 下 4 个段文件全部存在
11. 各段文件非空，不含 AI-template 模式
```

**f) 在步骤末尾添加占位符步骤，指示终稿拼接在 Plan 03-03 中处理**：

在 `verify_manuscript` 后、`checkpoint_confirm` 前插入：

```markdown
<step name="placeholder_concatenation" priority="medium">
注意：终稿拼接（合并各段 → 替换占位符 → 统一引用编号 → 生成 manuscript.md + YAML frontmatter + sections/ 输出）在 {{@./.planning/phases/03-手稿拼接/03-03-PLAN.md}} 中处理。本步骤仅确保各段独立文件就绪。

当前各段文件和引用库就绪后，进入 checkpoint_confirm。
</step>
```

**g) 更新 `<success_criteria>`**，追加分段完成标准：

在现有标准后追加：
- 各段（Introduction/Methods/Results/Discussion）独立完成引用和撰写
- 每段写入 05_Manuscript/sections/ 独立文件
- 每段撰写前 reference-agent 完成文献搜索
- 每段撰写后用户完成审阅
- 引用库引用不重复
- 各段使用占位符进行交叉引用
- 全文各段合计 >5000 字
</action>
<acceptance_criteria>
- grep -c "sequential_section_writing" pipeline/workflows/writing.md 返回 1（新步骤名存在）
- grep -c "Step A\|Step B\|Step C" pipeline/workflows/writing.md 返回 >= 3（三步循环）
- grep -c "reference-agent.*搜索\|reference.*pre-search\|Reference-Agent.*文献" pipeline/workflows/writing.md 返回 >= 1（每段前搜索）
- grep -c "05_Manuscript/sections/" pipeline/workflows/writing.md 返回 >= 2（各段输出目录）
- grep -c "01-introduction\|02-methods\|03-results\|04-discussion" pipeline/workflows/writing.md 返回 >= 4（各段文件命名）
- grep -c "humanizer" pipeline/workflows/writing.md 返回 >= 1（humanizer 保留但内嵌）
- grep -c "placeholder_concatenation" pipeline/workflows/writing.md 返回 >= 1（拼接占位符步骤）
- grep -c "reference-library.md" pipeline/workflows/writing.md 返回 >= 1（引用规范引用）
- grep -c "D-01\|D-02\|D-03\|D-04\|D-05\|D-06" pipeline/workflows/writing.md 返回 >= 4（移除的 draft_imrad_chapters 步骤替换后，至少有 4 个 D 决策引用）
- grep -c "checkpoint" pipeline/workflows/writing.md 返回 >= 2（用户审阅暂停）
- 命令 grep -c "draft_imrad_chapters" pipeline/workflows/writing.md 返回 0（旧步骤被移除）
</acceptance_criteria>
<verify>
  <automated>grep -c "sequential_section_writing" pipeline/workflows/writing.md</automated>
  <automated>grep -c "05_Manuscript/sections/" pipeline/workflows/writing.md</automated>
  <automated>grep -c "reference-library.md" pipeline/workflows/writing.md</automated>
  <automated>grep -c "placeholder_concatenation" pipeline/workflows/writing.md</automated>
</verify>
<done>writing.md 工作流从单次 IMRAD 全篇撰写改造为逐段顺序流水线，包含 reference-agent 预搜索、writer-agent 分轮撰写、用户审阅 pause、共享引用库集成</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → workflow | 用户审阅 checkpoint 的输入影响是否进入下一段 |
| reference-agent → reference_library.json | 文献搜索写入共享引用库 |
| writer-agent → 05_Manuscript/sections/ | 各段撰写写入 sections 目录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | DoS | 用户审阅 checkpoint 无限等待 | mitigate | checkpoint 输出明确命令（`通过`/`继续`→下一段，其他→修改当前段），用户不输入则保持在当前 checkpoint |
| T-03-05 | Tampering | 共享引用库在不同段落写入时交叉污染 | mitigate | 引用库写入使用去重规则（1.3），只追加新条目不修改已有条目。sections_used 字段合法追加段落名 |
| T-03-06 | DoS | 段间依赖导致某段卡住整条流水线 | accept | 分段设计本身确保各段独立，一段卡住不阻塞其他段的上下文准备。但流水线顺序是用户选择的（D-01），不能并行跳段 |
</threat_model>

<verification>
1. `pipeline/workflows/writing.md` 不再包含 `draft_imrad_chapters` 作为单一步骤
2. `sequential_section_writing` 步骤包含 Step A/B/C 三阶段
3. 每段撰写前 reference-agent 搜索步骤存在
4. 每段撰写后用户审阅 checkpoint 存在
5. 各段文件名明确为 01-introduction.md ~ 04-discussion.md
6. 引用库参考逻辑存在（reference-library.md 引用）
7. Humanizer 检查保留，内嵌到各段撰写中
8. 终稿拼接有占位符步骤指向 Plan 03-03
9. 上下文读取规则覆盖各段
</verification>

<success_criteria>
- [ ] `grep -c "sequential_section_writing" pipeline/workflows/writing.md` >= 1
- [ ] `grep -c "05_Manuscript/sections/" pipeline/workflows/writing.md` >= 2
- [ ] `grep -c "reference-library.md" pipeline/workflows/writing.md` >= 1
- [ ] `grep -c "01-introduction\|02-methods\|03-results\|04-discussion" pipeline/workflows/writing.md` >= 4
- [ ] `grep -c "checkpoint" pipeline/workflows/writing.md` >= 2
- [ ] Workflow 包含 Step A/B/C 三段式结构
- [ ] Humanizer 自检保留（内嵌于各段）
- [ ] 占位符步骤指向 Plan 03-03
- [ ] 各段上下文来源明确
</success_criteria>

<output>
After completion, create `.planning/phases/03-手稿拼接/03-01-SUMMARY.md`
</output>
