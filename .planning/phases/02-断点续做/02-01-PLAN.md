---
phase: 02-断点续做
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/clinpub/do.md
autonomous: true
requirements: [NEXT-01]

must_haves:
  truths:
    - "用户不带参数执行 `/clinpub-do` 时，看到当前 Phase 状态摘要和 1-3 条建议命令"
    - "用户带 NL 参数（如 `/clinpub-do 我想改清洗逻辑`）时，命令按意图路由到对应 Phase 命令"
    - "用户输入的 NL 无法推断意图时，命令回退到无参行为（显示状态摘要）"
    - "路由完成后，用户确认后才执行目标命令（不自动运行）"
    - "命令基于三合一信息做决策：STATE.md 状态 + 工件检测 + 可选的 NL 输入"
  artifacts:
    - path: "commands/clinpub/do.md"
      provides: "Workspace state router with NL intent detection"
      contains: "clinpub:do"
  key_links:
    - from: "commands/clinpub/do.md"
      to: ".planning/STATE.md"
      via: "读取 `- 阶段：Phase N` 行确定当前 Phase"
      pattern: "阶段：Phase"
    - from: "commands/clinpub/do.md"
      to: "project_config.yml"
      via: "检测项目是否已初始化"
      pattern: "project_config.yml"
    - from: "commands/clinpub/do.md"
      to: "02_PreprocessedData/data/cleaned.csv"
      via: "检测 Phase 1 是否完成"
      pattern: "cleaned.csv"
    - from: "commands/clinpub/do.md"
      to: "04_Outputs/"
      via: "检测 Phase 2 是否有分析输出"
      pattern: "04_Outputs"
    - from: "commands/clinpub/do.md"
      to: "05_Manuscript/manuscript.md"
      via: "检测 Phase 3 是否完成"
      pattern: "manuscript.md"
---

<objective>
创建 `/clinpub-do` 命令入口，实现工作区状态自动检测 + NL 意图路由

Purpose: 用户不需要记住具体命令名称。执行 `/clinpub-do` 时，命令自动读取 STATE.md、检测关键工件、处理可选的 NL 输入，三合一决策后输出当前状态摘要和建议命令，确认后路由到对应的 Phase 命令。

Output:
- `commands/clinpub/do.md` — 完整的路由命令入口，包含参数解析、状态检测、工件检测、NL 路由决策树、用户确认流程
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@commands/clinpub/data-prep.md
@commands/clinpub/analysis.md
@commands/clinpub/writing.md
@commands/clinpub/review.md
@commands/clinpub/init-project.md
@commands/clinpub/milestone.md
@.planning/STATE.md
@.planning/phases/02-断点续做/02-CONTEXT.md
@.planning/phases/02-断点续做/02-RESEARCH.md

<!-- 关键决策 (from CONTEXT.md) -->
<!-- D-01: 无参数 → 报告状态摘要 + 建议命令，确认后执行 -->
<!-- D-02: 有 NL 参数 → NL 优先于状态检测，直接按意图路由 -->
<!-- D-03: 路由依据三合一：STATE.md + 工件检测 + 可选 NL -->
<!-- D-04: NL 推断不出 → 回退到无参行为 -->
</context>

<tasks>

<task type="auto">
<name>Task 1: 创建 do.md 命令入口结构和参数解析逻辑</name>
<files>commands/clinpub/do.md</files>
<read_first>
- `commands/clinpub/init-project.md` — 最简命令入口格式模板 (34 行，frontmatter + objective + process + success_criteria)
- `commands/clinpub/data-prep.md` — 带重新进入检测的命令，含 project_config.yml 验证模式
- `.planning/phases/02-断点续做/02-CONTEXT.md` — D-01, D-02, D-03, D-04 决策细节
- `.planning/phases/02-断点续做/02-RESEARCH.md` — 第 129-168 行的路由决策树，第 242-276 行的三合一状态检测 Pattern 1，第 405-471 行的命令骨架
</read_first>
<action>
创建 `commands/clinpub/do.md`，写入以下内容：

1. **frontmatter**（与现有命令保持一致格式）:

```yaml
---
name: clinpub:do
description: "Read workspace state and auto-route to the appropriate clinpub command. With natural language input (e.g., '/clinpub-do 我想改清洗逻辑'), routes by intent. With no arguments, shows current state summary and suggests next commands."
argument-hint: "[your intent or question]"
allowed-tools:
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Bash
  - AskUserQuestion
---
```

2. **`<objective>`** 直接描述 D-01/D-02 的核心行为：

```markdown
<objective>
Workspace state router. Reads STATE.md, detects artifacts, and routes to the correct Phase command.

## 行为模式

- **无参数**（D-01）: 读取 STATE.md + 检测关键工件 → 输出状态摘要 + 建议 1-3 条命令 → 用户确认后路由
- **有 NL 参数**（D-02）: NL 意图识别优先于状态检测 → 成功则直接路由 → 失败则回退到无参行为（D-04）
- **路由依据**（D-03）: 三合一决策：STATE.md `- 阶段：Phase N` + 工件检测 + 可选 NL 输入
</objective>
```

3. **`<execution_context>`** 引用所有可能路由到的命令:

```markdown
<execution_context>
@./commands/clinpub/init-project.md
@./commands/clinpub/data-prep.md
@./commands/clinpub/analysis.md
@./commands/clinpub/writing.md
@./commands/clinpub/review.md
@./commands/clinpub/next-step.md
@./commands/clinpub/milestone.md
@./pipeline/references/checkpoints.md
</execution_context>
```

4. **`<process>` 开始部分 — 参数解析 + NL 意图识别**：

```markdown
<process>

## 1. 解析输入参数

```
如果提供了参数 NL_INPUT：
  1. 提取参数中的自然语言文本（去除非语义的前缀词如 "帮我"、"我想"、"我要"）
  2. 执行 NL 意图推断（见 Step 1.1）
  3. 推断成功 → 直接跳转到「6. 路由到目标命令」
  4. 推断失败 → 跳转到「2. 无参行为：状态检测」（D-04 回退）

如果没有提供参数（无参调用）：
  1. 直接进入「2. 无参行为：状态检测」（D-01）
```

### 1.1 NL 意图推断规则（D-02）

使用强信号关键词优先匹配策略，非简单包含匹配。关键词按优先级从高到低排列：

| 优先级 | 关键词（任一命中即路由） | 路由到 | 特异度 |
|--------|-------------------------|--------|--------|
| 1 | `(初始化\|init\|开始\|创建项目\|新建)` | init-project | 高 — 非清洗/分析语境 |
| 2 | `(清洗\|clean\|数据质量\|缺失\|异常值\|cleaned)` | data-prep | 高 — 特异性数据处理术语 |
| 3 | `(分析\|统计\|结果\|图\|表\|analysis\|figure\|table\|回归\|生存\|ROC)` | analysis | 高 — 分析术语 |
| 4 | `(写\|稿\|手稿\|文献\|引用\|writing\|IMRAD\|论文\|manuscript)` | writing | 高 — 写作术语 |
| 5 | `(审稿\|review\|修订\|修改\|意见\|审阅\|response)` | review | 高 — 审稿术语 |
| 6 | `(推进\|下一步\|继续\|next\|advance)` | next-step | 高 — 推进术语 |
| 7 | `(状态\|摘要\|总览\|当前\|情况\|see\|status\|什么阶段)` | 回退到无参 | 明确表达"查看状态"意图，不是特定命令 |

**匹配规则**:
- 命中高优先级关键词立即路由，不继续检查低优先级（D-02：NL 优先于状态检测）
- 命中第 7 组关键词 → 执行无参状态检测流程（D-04：明确表达查看状态，回退到摘要）
- 没有命中任何关键词组 → 回退到无参行为（D-04：推断不出明确意图）
- 跨组冲突时（如同时包含"分析"和"写"）→ 检测文本中最先出现的关键词组
- **反模式规避**: 不要匹配停用词（"看"、"查"、"项目"、"做"），只匹配上表中特异性术语

### 1.2 命令名称映射

路由使用 SKILL.md 定义的命令名称格式：

| 路由目标 Phase | 命令名称 | 执行方式 |
|---------------|---------|---------|
| Phase 0 | `/clinpub-init-project` | 确认后提示用户执行 |
| Phase 1 | `/clinpub-data-prep` | 确认后提示用户执行 |
| Phase 2 | `/clinpub-analysis` | 确认后提示用户执行 |
| Phase 3 | `/clinpub-writing` | 确认后提示用户执行 |
| Phase 4 | `/clinpub-review` | 确认后提示用户执行 |
| 自动推进 | `/clinpub-next-step` | 确认后提示用户执行 |
| Phase 检查 | `/clinpub-milestone N` | 确认后提示用户执行 |

```

注意: 这里只写 `## 1. 解析输入参数` + `### 1.1 NL 意图推断规则` + `### 1.2 命令名称映射` 三个小节。Task 2 继续写状态检测和路由部分。
</action>
<acceptance_criteria>
- grep -c "name: clinpub:do" commands/clinpub/do.md 返回 1
- grep -c "argument-hint: " commands/clinpub/do.md 返回 1
- grep -c "NL 意图推断规则\|1.1 NL" commands/clinpub/do.md 返回 1
- grep -c "命令名称映射\|1.2 命令" commands/clinpub/do.md 返回 1
- grep -c "(初始化\|init\|开始\|创建项目\|新建)" commands/clinpub/do.md 返回 1（关键词表存在）
- grep -c "(清洗\|clean\|数据质量\|缺失\|异常值\|cleaned)" commands/clinpub/do.md 返回 1
- grep -c "(状态\|摘要\|总览\|当前\|情况\|see\|status\|什么阶段)" commands/clinpub/do.md 返回 1（第 7 组回退关键词）
- grep -c "D-02\|D-04" commands/clinpub/do.md 返回 >= 2（NL 优先 + 回退逻辑的决策引用）
- grep -c "# Phase 0" commands/clinpub/do.md 返回 1（命令名称映射表参考行）
</acceptance_criteria>
<verify>
  <automated>grep -n "name:" commands/clinpub/do.md</automated>
  <automated>grep -c "NL 意图推断规则" commands/clinpub/do.md</automated>
  <automated>grep -c "(初始化\|init\|开始\|创建项目\|新建)" commands/clinpub/do.md</automated>
  <automated>grep -c "(状态\|摘要\|总览\|当前\|情况\|see\|status\|什么阶段)" commands/clinpub/do.md</automated>
</verify>
<done>do.md 文件的 frontmatter、objective、execution_context、参数解析、NL 意图推断规则、命令名称映射表全部创建完成</done>
</task>

<task type="auto">
<name>Task 2: 实现状态检测、工件检测、路由决策树和用户确认流程</name>
<files>commands/clinpub/do.md</files>
<read_first>
- `commands/clinpub/do.md` — Task 1 刚创建的内容（现在包含了 frontmatter + objective + execution_context + 参数解析 + NL 规则）
- `hooks/clinpub-phase-boundary.sh` — 第 73-104 行的 check_data_exists() 函数，展示各 Phase 关键工件检测模式
- `.planning/STATE.md` — 第 20 行 `- 阶段：Phase 2` 标识行格式
- `.planning/phases/02-断点续做/02-RESEARCH.md` — 第 129-168 行路由决策树图，第 242-276 行三合一状态检测 Pattern 1
- `.planning/phases/02-断点续做/02-CONTEXT.md` — D-01（无参行为）、D-03（三合一路由）
</read_first>
<action>
在 `commands/clinpub/do.md` 的 `<process>` 中，延续 Task 1 的 `1.2 命令名称映射` 之后，添加以下完整的状态检测和路由逻辑：

```markdown

## 2. 无参行为：状态检测（D-01）

### 2.1 读取 STATE.md 获取当前 Phase

```
从 STATE.md 中匹配 `- 阶段：Phase N`（使用正则：/阶段：Phase\s*(\d)/）：
- 匹配成功 → 获取当前 Phase 编号（Phase 0-4）
- 匹配失败 → 检查 STATE.md 是否存在
  - STATE.md 不存在或没有匹配行 → 项目未初始化 → 跳转到「3. 无参路由: 未初始化」
```

**反模式规避**: 不依赖 emoji 计数或自然语言行（如 `**当前状态**: ...`）做判断。只用 `- 阶段：Phase N` 结构化行作为 Phase 的权威来源（与 workflow-guard.js D-02 逻辑一致）。

### 2.2 检测关键工件

```
使用 Bash 检测以下工件是否存在：

1. 检查项目是否已初始化:
   - `-f project_config.yml` — 项目配置存在
   - 验证关键字段非空: project.name != "项目名称", variables.outcome 非空, paths.raw_data 对应目录存在
   （复用 data-prep.md 的重新进入检测模式）

2. 检测各 Phase 完成状态:
   Phase 0: project_config.yml 存在且关键字段有效
   Phase 1: -f 02_PreprocessedData/data/cleaned.csv
   Phase 2: -d 04_Outputs && ls 04_Outputs/ 非空
   Phase 3: -f 05_Manuscript/manuscript.md
   Phase 4: -d 05_Manuscript/final && ls 05_Manuscript/final/ 非空
```

**反模式规避**: 
- 检测 Phase 2 完成状态时同时检查 `04_Outputs/` 和 `project_config.yml analysis_plan.waves`，避免仅依赖 waves 字段判断（Pitfall 2: waves 可能为空对象 `{}`）
- 不硬编码命令名称字符串（Pitfall 1: 使用 Phase 映射表）

### 2.3 路由决策树（D-03 三合一决策）

```
依据 Phase 编号 + 工件检测结果 + (可选的 NL 推断结果)，输出状态摘要和建议命令。

---

**STATE.md 不存在或无法解析 Phase**:

输出来初始化建议:
```
当前状态: 项目未初始化
检测结果: STATE.md 不存在或无 Phase 标识行
建议: /clinpub-init-project  → 初始化项目配置和目录结构
      /clinpub-data2idea    → 从数据中挖掘论文选题（无需初始化）
请输入对应的命令名称继续，或输入 'exit' 取消。
```

---

**Phase 0 检测结果**:

┌──────────────────────────────────────────────────────────────┐
│ 条件: STATE.md 显示 Phase 0                                  │
│                                                              │
│ ┌─ project_config.yml 存在且关键字段有效?                     │
│ │  ├─ YES → 输出:                                            │
│ │  │  当前状态: Phase 0 (项目初始化) — 已完成                │
│ │  │  检测到 project_config.yml，关键字段有效                │
│ │  │  建议:                                                  │
│ │  │    /clinpub-data-prep        → Phase 1 数据清洗         │
│ │  │    /clinpub-next-step        → 自动推进到下一步         │
│ │  │                                                         │
│ │  └─ NO  → 输出:                                           │
│ │     当前状态: Phase 0 (项目初始化) — 未完成                │
│ │     project_config.yml 不存在或关键字段为空                │
│ │     建议:                                                  │
│ │       /clinpub-init-project    → 初始化项目                │
└──────────────────────────────────────────────────────────────┘

**Phase 1 ~ 4 各 Phase 的检测结果格式**
（统一使用以下模板，替换 N 和 name）：

```
当前状态: Phase N (name) — [进行中|已完成]
检测到: [已完成的工件列表] | [缺失的工件列表]
建议:
  /clinpub-{command}    → [做什么]
  /clinpub-{command}    → [做什么]
  /clinpub-next-step    → 自动推进到下一阶段

输入对应的命令名称（如 clinpub-data-prep）执行，或输入 'exit' 取消。
```

具体各 Phase 的路由条件:

| 当前 Phase | 完成条件 | 已完成时建议 | 未完成时建议 |
|------------|---------|-------------|-------------|
| Phase 1 | cleaned.csv 存在 | data-prep (重入刷新), next-step | data-prep (继续清洗) |
| Phase 2 | 04_Outputs/ 非空 | analysis (继续分析), next-step | analysis (开始分析) |
| Phase 3 | manuscript.md 存在 | writing (继续撰写), next-step | writing (开始撰写) |
| Phase 4 | final/ 有输出 | review (继续修改), 全部完成庆祝 | review (开始审稿) |

**Phase 2 的特殊处理:**
在建议 analysis 命令时，检测 `project_config.yml analysis_plan.waves`：如果 `waves: {}`（空），在建议中注明"尚未定义 Wave 结构，需要从基线描述开始"；如果 waves 非空，注明"已有 {N} 个 Wave，继续分析"。

---

### 2.4 输出格式规范

使用统一模板输出状态摘要：

```markdown
---

### 当前状态

| 维度 | 状态 |
|------|------|
| 当前 Phase | Phase {N}: {name} |
| 项目初始化 | ✅ 已完成 / ⏳ 未完成 |
| 关键工件 | {工件状态描述} |
| 建议命令 | 见下方 |

{具体的检测结果和 1-3 条建议命令}

---
```

---

## 3. 用户确认流程（D-01）

输出状态摘要和建议命令后，等待用户输入：

1. 用户输入命令名（如 `data-prep`）→ 提示用户执行 `/clinpub-{name}`
2. 用户输入 `exit` 或 `取消` → 结束命令
3. 用户输入其他内容 → 重新解释可用的命令选项

**重要**: 命令只做路由建议，不自动执行目标命令。用户需要手动输入目标命令执行。
</process>
```

在 `<objective>` 和 `<process>` 之间添加 `<interfaces>` 块，引用关键的正则和检测模式：

```markdown
<interfaces>
<!-- 关键检测模式，executor 直接使用 -->

STATE.md Phase 读取:
```
正则: /阶段：Phase\s*(\d)/
源:   hooks/clinpub-workflow-guard.js:31（已验证）
```

工件检测模式（复用 phase-boundary.sh:73-104）:
```
Phase 0: -f project_config.yml + 验证 project.name != "项目名称"
Phase 1: -f 02_PreprocessedData/data/cleaned.csv
Phase 2: -d 04_Outputs && ls 04_Outputs/ 非空 + project_config.yml analysis_plan.waves
Phase 3: -f 05_Manuscript/manuscript.md
Phase 4: -d 05_Manuscript/final && ls 05_Manuscript/final/ 非空
```
</interfaces>
```

在 `<process>` 之后添加 `<success_criteria>`：

```markdown
<success_criteria>
- 无参数时输出准确的当前 Phase 状态摘要 + 1-3 条建议命令
- 带 NL 输入时正确推断意图并路由到对应命令
- NL 推断失败时正确回退到无参行为（显示状态摘要）
- 路由后等待用户确认，不自动执行目标命令
- 所有 8 个命令的路由映射完整（init-project, data-prep, analysis, writing, review, next-step, milestone, data2idea）
- 不匹配停用词（"看"、"查"、"项目"、"做"）
</success_criteria>
```
</action>
<acceptance_criteria>
- grep -c "### 2.1 读取 STATE.md" commands/clinpub/do.md 返回 1（状态检测小节）
- grep -c "### 2.2 检测关键工件" commands/clinpub/do.md 返回 1（工件检测小节）
- grep -c "### 2.3 路由决策树" commands/clinpub/do.md 返回 1（路由决策小节）
- grep -c "### 2.4 输出格式规范" commands/clinpub/do.md 返回 1（输出格式小节）
- grep -c "cleaned.csv" commands/clinpub/do.md 返回 >= 1（Phase 1 工件检测）
- grep -c "04_Outputs" commands/clinpub/do.md 返回 >= 1（Phase 2 工件检测）
- grep -c "manuscript.md" commands/clinpub/do.md 返回 >= 1（Phase 3 工件检测）
- grep -c "# Phase\|Phase 0\|Phase 1\|Phase 2\|Phase 3\|Phase 4" commands/clinpub/do.md 返回 >= 5（所有 Phase 覆盖）
- grep -c "D-01\|D-02\|D-03\|D-04" commands/clinpub/do.md 返回 >= 4（所有决策引用存在）
- grep -c "<success_criteria>" commands/clinpub/do.md 返回 1
- grep -c "北匹配\|看\|查\|项目\|做" commands/clinpub/do.md 返回 0（不含停用词问题，这个 grep 可能误匹配，只是确认）
</acceptance_criteria>
<verify>
  <automated>grep -c "### 2.1 读取 STATE.md" commands/clinpub/do.md</automated>
  <automated>grep -c "cleaned.csv" commands/clinpub/do.md</automated>
  <automated>grep -c "D-01\|D-02\|D-03\|D-04" commands/clinpub/do.md</automated>
</verify>
<done>do.md 完整创建：包含参数解析、NL 意图推断、STATE.md 状态读取、6 种工件检测、各 Phase 路由决策树、统一输出格式、用户确认流程</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → NL parser | 用户提供的 NL 参数被解析为命令意图 |
| NL parser → command execution | NL 推断结果决定路由目标 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | NL 输入中的关键词绕过 | mitigate | 使用强信号白名单匹配（8 组特异性关键词），不将用户输入直接拼接为 shell 命令。路由只输出命令名称，由用户确认后手动执行 |
| T-02-02 | Spoofing | STATE.md 被篡改导致错误路由 | mitigate | workflow-guard.js 的 phase-guard 保护 .planning/ 目录写入安全；路由三合一决策（Phase + 工件 + NL）降低单一信息源错误风险 |
| T-02-03 | DoS | NL 匹配过度导致路由到错误命令 | accept | D-04 规定无明确匹配时回退到无参行为（显示摘要）。误匹配通过强信号词优先级机制最小化 |
</threat_model>

<verification>
1. `commands/clinpub/do.md` 存在且格式正确（frontmatter + objective + interfaces + process + success_criteria）
2. NL 意图推断包含 8 组关键词映射（init-project, data-prep, analysis, writing, review, next-step, 无参回退）
3. 状态检测包含 5 个 Phase 的工件检测逻辑
4. 无参行为正确输出状态摘要 + 建议命令
5. NL 推断失败回退到无参行为逻辑存在
6. 所有 D-01 到 D-04 决策被引用和实现
</verification>

<success_criteria>
- [ ] `grep -c "name: clinpub:do" commands/clinpub/do.md` >= 1
- [ ] `grep -c "D-01\|D-02\|D-03\|D-04" commands/clinpub/do.md` >= 4
- [ ] `grep -c "cleaned.csv" commands/clinpub/do.md` >= 1
- [ ] `grep -c "04_Outputs" commands/clinpub/do.md` >= 1
- [ ] NL 意图关键词表包含所有 8 个命令的路由映射
- [ ] 状态检测包含各 Phase 的关键工件路径
- [ ] 无参行为逻辑包含"输出摘要 → 建议命令 → 用户确认"三阶段
</success_criteria>

<output>
After completion, create `.planning/phases/02-断点续做/02-01-SUMMARY.md`
</output>
