---
phase: 04-方法增强
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/reference-agent.md
  - pipeline/workflows/analysis.md
  - pipeline/references/analysis_methods.md
autonomous: true
requirements: [METH-01]

must_haves:
  truths:
    - "用户在任何 Phase 中提及未知统计方法（如"帮我查一下 X 方法"）时，系统自动搜索并总结（D-01, D-03）"
    - "搜索优先使用 Tavily 获取方法概览，学术资料不足时再用 PubMed 补充（D-04）"
    - "首次输出方法简介 + 适用场景 + 2-3 篇关键参考文献摘要（D-05，自适应深度）"
    - "用户追问更多细节时再深入原理、R 实现、示例代码（D-05 深入层）"
    - "在 Phase 2 分析讨论阶段，搜索结果的摘要直接替换原 analysis spec 的方法描述部分（D-06 双轨制）"
    - "搜索返回的详细教程作为附件文档单独输出，不修改原方案（D-06 附件轨）"
    - "reference-agent 的 literature_search 步新增 method_search 模式专门处理统计方法查询"
  artifacts:
    - path: "agents/reference-agent.md"
      provides: "Extended reference-agent with method_search mode"
      contains: "method_search"
    - path: "pipeline/workflows/analysis.md"
      provides: "Updated analysis workflow with method search trigger"
      contains: "unknown_method\|method_search"
    - path: "pipeline/references/analysis_methods.md"
      provides: "Updated methods reference indicating dynamic search capability"
      contains: "未知方法"
  key_links:
    - from: "agents/reference-agent.md"
      to: "scripts/tavily_search.py"
      via: "method_search 模式优先调用 Tavily 搜索方法概览"
      pattern: "tavily_search"
    - from: "agents/reference-agent.md"
      to: "scripts/pubmed_search.py"
      via: "method_search 模式用 PubMed 补充学术引用"
      pattern: "pubmed_search"
    - from: "pipeline/workflows/analysis.md"
      to: "agents/reference-agent.md"
      via: "分析讨论阶段检测到未知方法时调用 reference-agent method_search"
      pattern: "reference-agent"
    - from: "pipeline/workflows/analysis.md"
      to: "pipeline/references/comparison-methods.md"
      via: "讨论组间对比时建议查阅 comparison-methods.md"
      pattern: "comparison-methods"
---

<objective>
扩展 reference-agent 增加 `method_search` 模式，实现未知统计方法的自动搜索与自适应呈现，并将该能力集成到分析工作流的讨论环节中。

Purpose: 实现 METH-01（未知统计方法自动搜索）。当前 reference-agent 仅有 `literature_search` 模式（搜文献做引用），缺乏针对"统计方法本身"的搜索能力。当用户在全管线任何阶段提到不熟悉的统计方法时，系统应：
1. 优先用 Tavily 搜索方法概览 + 应用场景（D-04）
2. 学术资料不足时用 PubMed 补充引用（D-04）
3. 首次输出摘要级信息，用户追问再深入（D-05）
4. 在 Phase 2 讨论时用双轨制集成（D-06）
5. 不维护白名单——由 reference-agent 动态判断（D-02）

Output:
- `agents/reference-agent.md` — 新增 method_search 步和对应的 execution_flow
- `pipeline/workflows/analysis.md` — 在讨论环节增加方法搜索触发提示
- `pipeline/references/analysis_methods.md` — 在 §二 Step 2 追加注记说明动态搜索能力
</objective>

<execution_context>
@./pipeline/workflows/execute-plan.md
@./pipeline/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-方法增强/04-CONTEXT.md (D-01 ~ D-07 决策详情)
@.planning/phases/04-方法增强/04-01-PLAN.md (concurrent plan for comparison-methods.md)

# 现有的参考 agent 合约
@agents/reference-agent.md

# 分析工作流（主要集成点）
@pipeline/workflows/analysis.md

# 搜索工具
@scripts/tavily_search.py — 方法概览搜索工具
@scripts/ncbi_search.py — PubMed 学术搜索
@scripts/pubmed_search.py — PubMed 搜索封装

<interfaces>
From agents/reference-agent.md:
- 已有结构: YAML frontmatter + <role> + <execution_flow> + <critical_rules> + <success_criteria>
- 已有步: check_api_keys → literature_search → full_text_retrieval → output_generation
- 输出文件: citation_map.md, references.bib, MANIFEST.yaml

From scripts/tavily_search.py:
```python
def search(query, api_key, search_depth="basic", topic="general", max_results=5,
           include_answer=True, ...) -> dict
# Returns: { "success": bool, "answer": str, "results": [...], ... }
```

From pipeline/workflows/analysis.md:
- <step name="propose_analysis_plan"> — 分析方案讨论环节
- <step name="discuss_and_confirm"> — 与用户讨论确认环节
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 reference-agent 增加 method_search 模式</name>
  <files>
    agents/reference-agent.md
  </files>
  <read_first>
    agents/reference-agent.md (完整读取当前结构)
    .planning/phases/04-方法增强/04-CONTEXT.md (D-01 ~ D-07)
  </read_first>
  <action>
    在 `agents/reference-agent.md` 中扩展以下内容。特别重要：找到 `literature_search` 步骤（约 line 35-55），在其之后插入新步骤。不要删除或修改现有 `literature_search` 步骤。

    **1. 在 `<execution_flow>` 中，`literature_search` 步之后新增：**

    ```xml
    <step name="method_search" priority="high">
    统计方法搜索模式。当用户在全管线任何阶段提及未知统计方法时触发（D-03）。

    **触发条件（D-03）：**
    - 用户明确提及方法名称（如"这个方法我不太熟悉"、"帮我查一下 X 方法"）
    - 不主动猜测、不扫描对话做预触发

    **搜索策略（D-04）：**
    1. **Tavily 优先**：调用 `scripts/tavily_search.py` 搜索方法概述、适用场景、优缺点
       ```bash
       python scripts/tavily_search.py "{method_name} statistical method overview application" --depth advanced --max-results 5
       ```
    2. **PubMed 补充**：如果 Tavily 返回的学术引用不足（<2 篇有 DOI 的引用），调用 PubMed 搜索
       ```bash
       python scripts/pubmed_search.py "{method_name} statistical method" --max 5 --type review
       ```
       - 或对罕见方法用 `--type clinical_trial` 找到应用案例

    **呈现格式（D-05，自适应深度）：**
    
    **第一层 — 摘要级（默认）：**
    ```
    ## 📊 {Method Name}
    
    **一句话概述：** {1-2 句描述该方法做什么}
    
    **适用场景：**
    - {场景 1}
    - {场景 2}
    
    **前提条件：**
    - {条件 1（如正态性、样本量要求）}
    
    **关键参考文献：**
    1. {Author Year} {Title} — DOI: {doi}（核心方法论文献）
    2. {Author Year} {Title} — DOI: {doi}（应用案例）
    3. {Author Year} {Title} — DOI: {doi}（综述或教程）
    
    **是否需要深入了解？** 我可以进一步提供 R 代码实现、参数详解、示例数据和结果解读。
    ```

    **第二层 — 深入层（仅当用户追问时）：**
    ```
    ## {Method Name} — 详细教程
    
    ### 原理
    {2-3 段原理说明，含数学公式（Markdown 格式）}
    
    ### R 实现
    ```r
    # 标准调用方式
    library({pkg})
    result <- {function}({params})
    
    # 参数详解
    # - param1: 说明
    # - param2: 说明
    ```
    
    ### 示例数据与输出解读
    {模拟数据或真实案例的运行结果和解读}
    
    ### 注意事项
    - {常见陷阱}
    - {替代方法/敏感性问题}
    ```

    **双轨制输出（D-06）：**
    1. **摘要轨**：摘要文本直接替换/插入到 analysis spec 的方法描述部分
    2. **附件轨**：详细教程作为 `.planning/phases/XX-name/attachments/{method-name}-tutorial.md` 单独输出，不修改原方案
    
    写入文件后，在 `MANIFEST.yaml` 中声明 tutorial 文件路径。
    </step>
    ```

    **2. 在 `<critical_rules>` 中追加一条：**
    
    ```markdown
    - 方法搜索优先 Tavily 再 PubMed（D-04），不能跳过 Tavily 直接搜 PubMed
    - 首次输出必须控制在摘要级（1-2 句话 + 3 个场景 + 2-3 篇文献），用户追问再展开
    - 不要编造参考文献——如果搜索不到结果，报告"未找到相关文献"
    - 如果用户未明确请求（如只是提了一句方法名但没有说"查一下"），不要触发 method_search
    ```

    **3. 在 `<success_criteria>` 中追加一条：**
    ```markdown
    - method_search 输出符合 D-05 自适应深度格式（摘要级 + 可选的深入层）
    - 摘要级包含：一句话概述 + 适用场景 + 关键参考文献
    - 深入层包含：原理 + R 代码 + 示例解读 + 注意事项
    - 输出的文献有真实 DOI（如搜索不到则注明）
    ```
  </action>
  <verify>
    <automated>bash -c "Select-String -Path 'agents/reference-agent.md' -Pattern 'method_search' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    1. `agents/reference-agent.md` 包含 `<step name="method_search">` 块
    2. 包含 Tavily 优先的搜索策略描述（"Tavily 优先" 或 "tavily_search"）
    3. 包含 PubMed 补充搜索描述
    4. 包含自适应呈现格式（摘要级 + 深入层两级描述）
    5. 包含双轨制输出描述（摘要轨 + 附件轨）
    6. `critical_rules` 中包含方法搜索相关规则
    7. `success_criteria` 中包含 method_search 相关标准
  </acceptance_criteria>
  <done>reference-agent 已具备 method_search 完整模式：触发条件、搜索策略、自适应呈现、双轨制输出均已定义</done>
</task>

<task type="auto">
  <name>Task 2: 集成到分析工作流 + 更新方法选择表</name>
  <files>
    pipeline/workflows/analysis.md
    pipeline/references/analysis_methods.md
  </files>
  <read_first>
    pipeline/workflows/analysis.md (读取 propose_analysis_plan 和 discuss_and_confirm 步)
    pipeline/references/analysis_methods.md (读取 §二 Step 2 方法选择表)
  </read_first>
  <action>
    做两处集成修改：

    **A. 在 `pipeline/workflows/analysis.md` 的 `<step name="discuss_and_confirm">` 中（约 line 119-171）追加：**

    在 "Write confirmed plan" 步骤之前（约 line 130），追加方法搜索提示：

    ```markdown
    <!-- 方法搜索触发（Phase 4 方法增强） -->
    **方法搜索提示：**
    如果用户提到不熟悉的统计方法（如"这个方法我不太熟悉"、"帮我查一下 XX 检验"），
    在讨论过程中自动触发 `reference-agent` 的 `method_search` 模式（见 `agents/reference-agent.md §method_search`）：
    
    1. 调用 Tavily 搜索方法概览 + 适用场景
    2. 学术引用不足时用 PubMed 补充
    3. 输出摘要级结果直接替换 spec 的方法描述部分
    4. 用户追问时输出详细教程到 `attachments/{method-name}-tutorial.md`
    
    如果用户确认的对比方法涉及 2 组或 3+ 组比较，提示用户：
    1. 对比方法已标准化到 `pipeline/references/comparison-methods.md`
    2. 给出标准检验树（正态性→方差齐性→具体方法→效应量）
    ```

    **B. 在 `pipeline/references/analysis_methods.md` 的 §二 Step 2 方法选择表下方（约 line 53）追加注记：**

    ```markdown
    > **💡 未知方法？** 如果用户提到的统计方法不在本决策树中，系统会自动搜索方法概览并与用户讨论。
    > 参见 `agents/reference-agent.md` 的 `method_search` 模式。
    ```

    不要修改 comparison-methods.md 或其他文件。
  </action>
  <verify>
    <automated>bash -c "Select-String -Path 'pipeline/workflows/analysis.md' -Pattern 'method_search|comparison-methods.md' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    1. `pipeline/workflows/analysis.md` 的 discuss_and_confirm 步中包含 method_search 触发提示
    2. 提示包含 Tavily → PubMed 搜索路径描述
    3. 提示包含 comparison-methods.md 标准比较方法的引用
    4. `pipeline/references/analysis_methods.md` 的 §二 方法选择表下方有"未知方法"注记
  </acceptance_criteria>
  <done>分析工作流在讨论环节可感知方法搜索能力，方法选择表标明"未知方法可搜索"，组间对比可引用标准决策树</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent prompt → external API | reference-agent 的 search query 由开发者/用户对话产生，搜索 API（Tavily/NCBI）是只读调用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | I | method_search step | accept | 搜索 query 来自用户在 Claude Code 对话中提及的方法名，非外部输入，注入风险极低 |
| T-04-03 | S | Tavily search | mitigate | 使用 Python subprocess 调用脚本（非用户拼接命令），搜索词经过 CLI 参数化处理 |
| T-04-04 | S | PubMed search | mitigate | 同上，API key 通过环境变量传递，不暴露敏感信息 |
</threat_model>

<verification>
1. `grep -c "method_search" agents/reference-agent.md` >= 3（步骤定义 + 触发 + 输出格式）
2. `grep -c "Tavily\|tavily_search" agents/reference-agent.md` >= 1
3. `grep -c "tutorial" agents/reference-agent.md` >= 1（附件轨输出）
4. `grep -c "method_search\|comparison-methods" pipeline/workflows/analysis.md` >= 1
5. `grep -c "未知方法" pipeline/references/analysis_methods.md` >= 1
</verification>

<success_criteria>
- reference-agent 获得 method_search 模式，含完整的触发条件、搜索策略、自适应呈现、双轨制输出
- analysis.md 讨论环节可感知方法搜索能力并提示使用 comparison-methods.md
- analysis_methods.md 标明"未知方法可动态搜索"
- 所有变更遵循已有的 YAML frontmatter + XML 标签 + Markdown 混合格式
</success_criteria>

<output>
After completion, create `.planning/phases/04-方法增强/04-02-SUMMARY.md`
</output>
