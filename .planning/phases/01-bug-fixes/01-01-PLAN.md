---
phase: 01-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - hooks/clinpub-workflow-guard.js
autonomous: true
requirements: [BUG-01]

must_haves:
  truths:
    - "Hook 读取 STATE.md 中 `- 阶段：Phase N` 行时正确返回 Phase 编号"
    - "Hook 在 STATE.md 没有 `- 阶段：` 行时返回 Phase 0（未初始化状态）"
    - "STATE.md 同时保留 human-readable 的 `当前状态` 行供人阅读"
    - "原有的 emoji 回退逻辑被保留但不再作为主要相位的判定依据"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Machine-readable phase identifier line `- 阶段：Phase N`"
      contains: "Phase 1"
    - path: "hooks/clinpub-workflow-guard.js"
      provides: "Fixed getCurrentPhase() with structured regex"
      contains: "阶段：Phase\\s*(\\d)"
  key_links:
    - from: "hooks/clinpub-workguard-guard.js"
      to: ".planning/STATE.md"
      via: "getCurrentPhase() 读取 STATE.md 并匹配 `- 阶段：` 行"
      pattern: "getCurrentPhase"
---

<objective>
修复 BUG-01：Hook 正则无法识别 STATE.md 中的 `- 阶段：Phase N` 格式

Purpose: 当 STATE.md 使用 `- 阶段：Phase N` 格式时，`getCurrentPhase()` 应正确解析并返回当前 Phase 编号，不回退到 emoji 计数逻辑导致的错误 Phase。
Output:
- `.planning/STATE.md` — 确认 machine-readable 的阶段标识行格式正确
- `hooks/clinpub-workflow-guard.js` — 修复后的 `getCurrentPhase()` 函数
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-bug-fixes/01-CONTEXT.md

<!-- 关键决策引用 -->
<!-- D-01: STATE.md 头部加 "- 阶段：Phase N" 行 -->
<!-- D-02: 正则改为 /阶段：Phase\s*(\d)/ -->
<!-- D-03: 保留 human-readable "当前状态" 行 -->
<!-- D-04: 无结构化行时返回 Phase 0，保留原有回退逻辑 -->
</context>

<tasks>

<task type="auto">
<name>Task 1: STATE.md machine-readable 阶段标识行格式确认</name>
<files>.planning/STATE.md</files>
<read_first>
- `.planning/STATE.md` — 当前 STATE.md 内容（已有 `- 阶段：Phase 1` 在第 5 行）
- `.planning/phases/01-bug-fixes/01-CONTEXT.md` — D-01 决策：标识行放在 `当前状态` 一栏下方
</read_first>
<action>
读入 STATE.md 并确认第 5 行的 `- 阶段：Phase 1` 格式和位置正确：

1. 确认该行紧跟在 `**当前状态**: ...` 行之后（第 4 行），中间无空行或其他内容
2. 确认格式为 `- 阶段：Phase N`（中文冒号，冒号后一个空格，"Phase" 首字母大写，数字后无多余字符）
3. 如果当前行 `- 阶段：Phase 1` 在第 5 行（第 4 行是 `**当前状态**: ...`），则保持不动
4. 如果该行位置不正确（例如在其他位置），移到 `**当前状态**` 行的正下方
5. 确保第 4 行 `**当前状态**: ...` 自然语言行保持原样（D-03：保留作为 human-readable 信息）
</action>
<acceptance_criteria>
- grep -n -- "- 阶段：Phase" ".planning/STATE.md" 输出第 5 行: `- 阶段：Phase 1`
- 第 5 行内容为 `- 阶段：Phase 1`（精确匹配，无偏差）
- 第 4 行为 `**当前状态**: ...`（human-readable 行保留）
- grep -c -- "- 阶段：Phase" ".planning/STATE.md" 返回 1（确保只有一行匹配）
</acceptance_criteria>
<verify>
  <automated>grep -n -- "- 阶段：Phase" ".planning/STATE.md" | head -2</automated>
  <automated>grep -c -- "当前状态" ".planning/STATE.md"</automated>
</verify>
<done>STATE.md 在 `**当前状态**` 行正下方有且仅有一行格式正确的 `- 阶段：Phase 1`</done>
</task>

<task type="auto">
<name>Task 2: Hook 正则修复 — getCurrentPhase() 改为匹配结构化行</name>
<files>hooks/clinpub-workflow-guard.js</files>
<read_first>
- `hooks/clinpub-workflow-guard.js` — 当前 `getCurrentPhase()` 在第 25-37 行，第 31 行有旧正则 `/当前.*Phase\s*(\d)/i`
- `.planning/phases/01-bug-fixes/01-CONTEXT.md` — D-02（新正则）、D-04（回退逻辑保留）
</read_first>
<action>
修改 `hooks/clinpub-workflow-guard.js` 中的 `getCurrentPhase()` 函数（当前第 25-37 行）：

1. 将第 31 行的正则表达式从：
   ```javascript
   const phaseMatch = content.match(/当前.*Phase\s*(\d)/i);
   ```
   改为：
   ```javascript
   // D-02: 匹配结构化 machine-readable 行 "- 阶段：Phase N"
   const phaseMatch = content.match(/阶段：Phase\s*(\d)/);
   ```

2. 在第 32 行（匹配成功后的 `if (phaseMatch)` 块）上方添加注释：
   ```javascript
   // D-02: Authoritative source — match structured line only
   ```

3. 保留第 34-36 行的 emoji 回退逻辑（D-04："不清除原有的 ✅ 回退逻辑"），但在其上方添加注释：
   ```javascript
   // D-04: Legacy fallback — kept for backward compatibility.
   //       New code path above handles the structured line match.
   ```

4. 第 33 行当 `phaseMatch` 不匹配时，函数返回 `0`（D-04：未初始化状态）。当前代码中 fallback 返回的是 `completedMatches ? completedMatches.length : 0`，这符合 D-04 的要求——保留不修改。

5. 确保 `if (phaseMatch)` 分支返回 `parseInt(phaseMatch[1], 10)` 不变。
</action>
<acceptance_criteria>
- grep -n "phaseMatch = content.match" hooks/clinpub-workflow-guard.js 输出行包含 `/阶段：Phase\\s*(\\d)/`（严格匹配新正则，不包含 "当前" 关键字）
- grep -c "当前.*Phase" hooks/clinpub-workflow-guard.js 返回 0（旧正则已移除）
- grep -c "✅" hooks/clinpub-workflow-guard.js 返回 >= 1（回退逻辑保留）
- grep -c "D-02" hooks/clinpub-workflow-guard.js 返回 >= 1（新正则注释）
- grep -c "D-04" hooks/clinpub-workflow-guard.js 返回 >= 1（回退逻辑注释）
</acceptance_criteria>
<verify>
  <automated>grep -n "phaseMatch = content.match" hooks/clinpub-workflow-guard.js</automated>
  <automated>grep "✅" hooks/clinpub-workflow-guard.js | head -3</automated>
  <automated>grep -c "当前.*Phase" hooks/clinpub-workflow-guard.js</automated>
</verify>
<done>getCurrentPhase() 使用 `/阶段：Phase\s*(\d)/` 正则匹配，emoji 回退逻辑保留在后方</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STATE.md -> Hook | 不可信文件内容被可信 Hook 代码解析 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | STATE.md 被篡改导致 hook 误判 Phase | mitigate | 使用严格正则 `/阶段：Phase\s*(\d)/` 精确匹配结构化行，忽略非结构化的自然语言内容 |
| T-01-02 | DoS | STATE.md 缺少 `- 阶段：` 行时 hook 行为 | accept | D-04 规定缺少时返回 Phase 0（未初始化），这是保守的 fallback——阻止所有未来 Phase 写入而非错误放行 |
| T-01-03 | DoS | Hook 解析错误时 fallback 为 allow | accept | `workflow-guard.js:125-128` 的 try/catch 在解析错误时允许操作，风险低于阻断所有正常操作 |
</threat_model>

<verification>
1. STATE.md 包含 `- 阶段：Phase 1` 在第 5 行
2. Hook 中 `getCurrentPhase()` 不再使用旧正则 `/当前.*Phase/`
3. Hook 的 `if (phaseMatch)` 分支匹配结构化行
4. emoji 回退逻辑（`/✅/g`）仍然存在但不再是主要 Phase 判定依据
5. 通过模拟验证（如果在可执行环境中）：创建 mock STATE.md，调用 getCurrentPhase() 确认返回值
</verification>

<success_criteria>
- [ ] `grep -c "阶段：Phase.*\\\\d" hooks/clinpub-workflow-guard.js` >= 1
- [ ] `grep -c "当前.*Phase" hooks/clinpub-workflow-guard.js` == 0
- [ ] STATE.md 第 5 行为 `- 阶段：Phase 1`
- [ ] emoji 回退逻辑未被删除
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes/01-01-SUMMARY.md`
</output>
